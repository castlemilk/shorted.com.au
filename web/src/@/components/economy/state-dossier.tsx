"use client";

// TODO(Task 3): replace this stub with the real state dossier (per-state
// charts + top exports). Kept minimal so Task 2's explorer compiles.
export function StateDossier(_props: {
  state: string;
  metricKey: string;
  onClose: () => void;
}) {
  return null;
}
