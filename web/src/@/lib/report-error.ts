/**
 * Where data-dispute reports go.
 *
 * docs/influence-editorial-standards.md rule 8 requires a "report an error"
 * affordance on every influence surface, so this address is load-bearing rather
 * than decorative. It was duplicated as a local const in two industry components
 * before the politician surfaces needed a third copy.
 */
export const REPORT_ERROR_EMAIL = "support@shorted.com.au";
