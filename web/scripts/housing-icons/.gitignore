# Full-res source icons (~1.4MB each) generated by generate-icons.mjs.
# The packed sprite (web/public/housing-icons/housing-icons.png) is the shipped
# asset and IS committed; regenerate these originals with `MODE=live node
# generate-icons.mjs` if you need to re-pack at a different cell size.
out/
