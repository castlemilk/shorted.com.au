// Reuse the proven suburb-banner ramps so economy banners match the shipped
// light/source and dark gradient-map treatments exactly.
export {
  LIGHT_RAMP,
  DARK_RAMP,
  RAMPS,
  buildLut,
  THEME,
} from "../housing-banners/palette.mjs";
