.venv
venv
.vscode
shorted-dev*.json

# Test results
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.playwright-cli/
.playwright-mcp/

# Coverage reports
coverage/
*.lcov
coverage.out
coverage.html

# Environment variables
.env
.env.local
.env.*.local
.env.prod
.env.production

# Terraform state and sensitive files (but not the terraform/ directory itself!)
**/*.tfstate
**/*.tfstate.*
**/*.tfstate.backup
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfvars
!**/terraform.tfvars.example

# Binaries
docker-credential-*
shorts-service
market-data-service
services/bin/
*.exe
*.dll
*.so
*.dylib

# Environment files (use .env.example instead)
services/.env

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.pyc

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Log files
*.log
*.log.*

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~
20??-??-??-*-local-command-caveatcaveat-the-messages-below.txt*
.serena/
scripts/job-watchdog/
node_modules/
venv/
.vercel
.env*.local
.worktrees/

# Session verification screenshots land at the repo root — never committed.
/*.jpeg
/*.jpg
/*.png

# Jupyter checkpoint copies — an editor artifact, never source
.ipynb_checkpoints/

# /design-sync (claude.ai/design system sync)
# Durable, COMMITTED: .design-sync/{config.json,NOTES.md,conventions.md,gen-entry.mjs,previews/,overrides/}
# Everything below is regenerated by cfg.buildCmd or the converter.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
web/.ds-sync-entry.tsx
web/.ds-sync-process-shim.ts
web/.ds-sync-fonts.css
web/.ds-sync-css-header.css
web/.ds-sync-styles.css
web/.ds-sync-tw.css
web/.ds-sync-tsconfig.json
web/.ds-sync-paths.json
web/.ds-sync-next-image.tsx
web/.ds-sync-tailwind.config.ts
web/.ds-types/
web/index.d.ts
