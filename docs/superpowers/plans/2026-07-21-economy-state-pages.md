# Economy State Pages, Icons, GovFin & Correlations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Implementers run on Opus with maximum-depth reasoning instructed; reviews inherit the coordinator model (fable).

**Spec:** `docs/superpowers/specs/2026-07-21-economy-state-pages-design.md`
**Branch:** `feat/economy-state-pages` (worktree `~/projects/.worktrees/shorted-economy-map`). NEVER touch `~/projects/shorted` (another session owns it). Local DB localhost:5438 fully populated (incl. state_exposure + MV). Established conventions: economy plan docs from this branch's history; probe-truth-wins importers; fail-closed filters; serializable registries; ssr:false chains; --no-verify for the known hook failure.

## Task 1 (Opus): `/economy/[state]` routes + breadcrumbs + hub rewire
**Files:** create `web/src/app/economy/[state]/page.tsx` (+ small server components as needed), `web/src/@/components/economy/state-page-*.tsx` as needed; modify `economy-map-explorer.tsx` (click → router.push, ?state= → router.replace redirect, remove StateDossier mount), `state-dossier.tsx` (dissolve into state-page components — keep StateCompanies/TopExports as importable pieces), `web/src/app/sitemap.ts` (+8 URLs), delete dead code.
Steps: scout an existing breadcrumb pattern (grep breadcrumb/BreadcrumbList in web/src — reuse if found); build the page (generateStaticParams 8 slugs, notFound guard, revalidate 3600, metadata per state, SSR chip strip via getEconomicSeries, client grid below, locator-inset map via ChoroplethMap fitToId+interactive=false linking back); breadcrumbs UI + BreadcrumbList JSON-LD; hub rewire; jest + tsc; Playwright walk (hub click → page loads with breadcrumbs; direct URL; bogus slug 404; back-link); build check (routes listed as SSG ○ with the 8 params); commit.

## Task 2 (Opus): iconography + TopExports alignment
**Files:** create `web/scripts/economy-icons/` (clone housing-icons: icon-set.config.mjs with the ~24 spec icons + housing's STYLE suffix verbatim, generate-icons.mjs flow path, PLUS generate-icons-openai.mjs direct fallback modeled on web/scripts/housing-banners/generate-backgrounds-openai.mjs, pack-sprite.mjs, README), `public/economy-icons/` sprite + manifest, `web/src/@/components/economy/economy-icon.tsx` (clone HousingIcon); wire into tiles/ChartCard headers on hub + state pages + commodities rows; fix TopExports alignment (CSS grid columns).
Icon generation: FIRST check brandbrain#178 status (`gh pr view 178 -R skunkworq/brandbrain --json state`); if merged AND the rebuilt MCP exists, drive the flow (mock-run first, then live; landmines in housing-icons README + the brandbrain-mcp-icon-flow memory: single persistent MCP connection, GENERATE_TEXT_POLICY_DISALLOW, apply_flow_edits one PUT, provider openai/gpt-image-1 strategy direct); OTHERWISE use the direct OpenAI path (OPENAI_API_KEY from ~/projects/shorted/services/.env — read-only). Either path: generate all icons (resumable), pack sprite, commit sprite + manifest + tooling (out/ gitignored). Verify icons render in the app (Playwright) + alignment fix visually. Commit.

## Task 3 (Opus): ABS Government Finance importer (`-mode govfin`)
**Files:** `services/economy-collector/govfin.go` + `govfin_test.go`, `main.go` (new mode + in `all`), `sources.go` (+abs-government-finance).
Probe the SDMX catalog for the GFS dataflow (`curl .../rest/dataflow/ABS?detail=allstubs | grep -iE "GOVERNMENT|GFS"`), then probe dimensions; pin state general-government quarterly revenue/expenses/net-operating-balance (+ net debt if present); topic govfin, aud units (watch UNIT_MULT — verify with a cross-check like the SFD importer did), lfStates mapping, fail-closed, Dimensions provenance, fixture tests. Live smoke locally (-mode govfin → plausible values: NSW annualized revenue ~$100e9). If NO usable SDMX GFS flow exists (possible — GFS may be Excel-only like GSP): report findings and implement the best available alternative flow if one is close (e.g. quarterly state final consumption?) — otherwise STOP and report NEEDS_CONTEXT with the probe evidence; do NOT scrape non-SDMX sources this round. Wire into `-mode all`. Commit.

## Task 4 (Opus): `-mode markets` — exposure-weighted short-interest series
**Files:** `services/economy-collector/markets.go` + `markets_test.go`, `main.go`, `sources.go` (+derived-shorted-markets registry entry, licence "derived", public_enabled TRUE).
Pure-SQL derivation (no LLM): monthly (month-end trading day) per-state exposure-weighted avg short % — SQL over `shorts` history × `mv_company_state_exposure` (weights, market caps; exclude international; weight×mc weighting; companies with short data only), from 2015-01. Emit `markets.short_interest_wavg.{state}` (percent, monthly, adjustment original). The SQL is the heart: TDD the pure Go assembly around a fixtured query result AND validate the SQL itself against the local DB (spot-check WA latest ≈ plausible 1-3% given current miners). Idempotent; `-mode all` ordering note: markets AFTER nothing else in-run (reads shorts+MV, both already in DB). Commit.

## Task 5 (Opus): state-page correlations UI — dual-axis overlay + chips
**Files:** `web/src/@/lib/economy/correlation.ts` (+tests: pearson, monthly alignment incl. quarterly forward-fill), `web/src/@/components/economy/dual-axis-chart.tsx` (or reuse StockChart core if it cleanly supports two independent y-domains — scout first), `state-correlations.tsx` (fetch short-interest series + candidate series client-side, compute rolling 24m pearson, render top chips |r|≥0.4 + the overlay with a series switcher), wire into `/economy/[state]`; "correlation ≠ causation" footnote.
Verify with real local data (WA exports vs short interest should produce a meaningful r; screenshot). Jest + tsc + Playwright + build. Commit.

## Task 6: integration verification + PR (coordinator-driven)
Full suites (go + web), final whole-diff fable review, Playwright end-to-end walk (hub → WA page: breadcrumbs, icons, aligned commodities, finances charts if Task 3 landed, overlay + chips), push, PR (include: post-merge `gcloud run jobs execute economy-collector` note for govfin/markets first prod run + revalidate sweep). Merge per user's standing instruction, deploy-watch, prod ops (collector execute + revalidate — user has pre-authorized this round's pattern; if the classifier balks, hand over the script), live verify.

## Sequencing
1 → 2 → 3 → 4 → 5 → 6 strictly sequential (single worktree). Tasks 3/4 are backend-only but share main.go/sources.go — no parallelism.
