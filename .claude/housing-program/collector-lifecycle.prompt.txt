Implement the housing work package "collector-lifecycle" for the Shorted repo checkout you are in - a dedicated git worktree on branch feat/housing-collector-lifecycle, base commit 8c120a352.

FIRST read the full spec at /Users/benebsworth/projects/shorted/.claude/housing-program/collector-lifecycle.md and follow it exactly: the ground rules, the findings, the QA requirements, and the commit requirements. If you cannot read that file, stop and report that instead of guessing.

Scope reminder - the three findings: F02 silent exit-0 failures + missing housing-freshness sentinel workflow, F21 failed runs clobbering the source cursor, F20 parity/kill-switch tests not gating PRs.

Commit ALL your work on this branch with conventional-commit messages (one commit per logical unit is fine). Never push, never merge, never switch branches. End with a summary: changes per finding, the actual test results you observed, and what needs hand-verification.
