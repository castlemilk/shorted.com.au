Continue the review-fix round on this branch (feat/housing-mv-correctness). A previous run was interrupted; its work IS already committed as a5e70131c.

ALREADY FIXED - do not redo: the inverted crime-rank floor (F33). Ranks are now scanned as sql.NullFloat64 and floored in Go via displayCrimeRank, so NULL -> 0 (no data) and covered ranks floor at 0.1. Verify the tests around it assert BEHAVIOUR (0 for a no-data row) rather than matching a SQL string literal, and fix them if they still pin the old expression.

STILL OPEN - fix these, highest severity first. Full evidence for each is in /Users/benebsworth/projects/shorted/.claude/housing-program/fix-mv-correctness.md (read it; skip only its crime-rank items):

1. [BLOCKER] mv_agency_stats ranking is inverted. 000109 made dropped_count (and total_drop_value) NULL below the k=3 floor, but ListAgencyPriceStats orders by `dropped_count DESC, total_drop_value DESC` and Postgres DESC defaults to NULLS FIRST - so agencies with NO drops sort ABOVE the biggest discounters on /price-drops. Reproduced on a real Postgres container. Preferred fix: keep the COUNT non-NULL (restore COALESCE(...,0)) and suppress only the DEPTH columns (avg_drop_pct/total_drop_value), which is what F07's k-anon requirement actually asks for and what 000086 already did. If you keep any ordering key NULLable, every DESC term in the store's orderBy whitelist needs an explicit NULLS LAST.
2. [MAJOR] The 12-month sold window derives sold_at only from first_seen/status_change events, silently dropping sales recorded as a `relisted` event. Either include 'relisted' in the event_type filter or derive sold_at from MAX(observed_at) FILTER (WHERE listing_status = 'sold') across all event types.
3. [MAJOR] 000109 drops mv_agency_stats.agent_names while the deployed API still selects it. Under this repo's documented DDL-before-merge rollout order that breaks prod between the DDL apply and the code deploy. Keep an empty `'{}'::text[] AS agent_names` placeholder column in the rebuilt MV (preferred), or read it shape-agnostically.
4. [MINOR] The F05 caller-side `SET statement_timeout = 0` fix was applied to services/house-price-collector/store.go but NOT to its near-identical fork at services/jobs/internal/jobs/houseprices/store.go, so the fix silently un-applies at the jobs-binary cutover. Apply it there too and extend the migration test to assert both files.
5. [MINOR] The k=3 floor on mv_suburb_price_drops is arithmetically invertible: publishing count + avg + median + max_drop_pct + max_drop_abs on the same row can reconstruct individual listing cuts. Stop publishing the extremes alongside count+mean+median on a k-floored row (drop the max_* columns, raise the floor for rows carrying extremes, or bucket the published statistics).
6. [MINOR] The only behavioural test for F17 (politician_property_count) cannot execute - the integration test is already red before the assertion is reached because the test DDL lacks the banner_* columns and a stub mv_suburb_crime_latest. Make it actually run, or cover F17 with a test that genuinely exercises the scan order.

Ground rules: add fix commits on top of the existing history; do not rewrite history, push, merge or switch branches. If you add or change a migration, keep numbering within 000107-000110 and restate the exact prod hand-apply order in your summary. Fix root causes; where a test pins buggy behaviour, fix the test to assert real behaviour. Re-run scoped tests and report ACTUAL output - if the sandbox blocks something, say so plainly. If you believe a finding is wrong, argue it with evidence rather than skipping it silently.
