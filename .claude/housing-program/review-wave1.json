{
 "confirmed": [
  {
   "target": "mv",
   "finding": {
    "severity": "blocker",
    "file": "services/shorts/internal/store/shorts/postgres_house_prices.go",
    "line": "302-304",
    "title": "F33 crime-rank floor inverts its own contract: GREATEST ignores NULL, so \"no data\" now returns 0.1 instead of 0",
    "detail": "`COALESCE(GREATEST(ROUND(cr.break_ins_rank::numeric, 1), 0.1), 0)` puts GREATEST *inside* the COALESCE. Postgres GREATEST/LEAST ignore NULL operands and only return NULL when every argument is NULL, so `GREATEST(NULL, 0.1)` = 0.1 and the outer COALESCE never fires. Verified on PG 17.6:\n\n    SELECT COALESCE(GREATEST(ROUND(NULL::numeric,1), 0.1), 0);  -- 0.1\n\nand end-to-end against a LEFT JOIN with no matching crime row:\n\n    sal_code                 | new_behaviour | old_behaviour\n    NSW-COVERED              |           0.1 |           0.0\n    VIC-SUBURB-NO-CRIME-DATA |           0.1 |             0\n\nSo the change does the exact opposite of its stated goal: it makes 0 *unreachable* and collapses \"covered, lowest rank\" and \"no data\" onto the same value. The `listStateSuburbsCrimeJoin` (line 259-271) is a LEFT JOIN whose inner query re-asserts `NOT small_pop AND NOT unreliable`, so every suburb outside the loaded jurisdictions AND every deliberately quarantined small-population/unreliable suburb yields NULL ranks here. The added test `TestListStateSuburbsQuery_SeparatesCoveredLowRanksFromNoData` (postgres_house_prices_query_test.go:44) asserts the buggy string literally and so locks the defect in.",
    "failure_scenario": "Crime data is Phase-1 NSW only. Open /housing/VIC and pick \"Colour by \u2192 Break-ins\". Every one of the ~3,000 VIC suburbs has no row in mv_suburb_crime_latest, so cr.break_ins_rank is NULL, so ListStateSuburbs returns crimeBreakInsRank = 0.1. web/src/@/lib/housing/highlight-metrics.ts:289 (`s.crimeBreakInsRank > 0 ? s.crimeBreakInsRank : null`) therefore treats them as covered and the choropleth paints the entire state at the safe end of the yellow\u2192red danger ramp instead of the no-data hatch \u2014 a fabricated crime percentile published for suburbs with zero crime data, including the small_pop/unreliable rows the 000092 gate exists to withhold.",
    "fix": "Move the floor inside the null-check, e.g. `CASE WHEN cr.break_ins_rank IS NULL THEN 0 ELSE GREATEST(ROUND(cr.break_ins_rank::numeric, 1), 0.1) END` (or `COALESCE(GREATEST(ROUND(...), 0.1) * (cr.break_ins_rank IS NOT NULL)::int, 0)`), and change the test to assert 0-for-NULL rather than the literal expression."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "blocker",
    "note": "CONFIRMED, blocker. Verified in /Users/benebsworth/projects/.worktrees/shorted-hw-mv-correctness/services/shorts/internal/store/shorts/postgres_house_prices.go:302-304 (added by 8652951b5 vs 8c120a352).\n\nSQL semantics reproduced on a throwaway PostgreSQL 17.10 container (removed after): SELECT COALESCE(GREATEST(ROUND(NULL::numeric,1), 0.1), 0) => 0.1; GREATEST(NULL::numeric, 0.1) => 0.1. GREATEST ignores NULL operands, so the outer COALESCE is dead code and 0 is unreachable.\n\nEnd-to-end reproduction against a mock of listStateSuburbsCrimeJoin (same MAX(...) FILTER subquery + \"WHERE NOT small_pop AND NOT unreliable\" + LEFT JOIN):\n  NSW-LOWEST (pct_rank 0.006) -> new 0.1 / old 0.0\n  NSW-SMALLPOP (gated out)    -> new 0.1 / old 0\n  VIC-NODATA (no row)         -> new 0.1 / old 0\nAll three collapse to 0.1; covered-lowest and no-data are no longer separable, which is the exact opposite of the new test's name.\n\nReachable, and no guard exists anywhere in the diff or downstream:\n- services/migrations/000090_add_suburb_crime.up.sql:23 states the contract verbatim: \"No-data jurisdictions (TAS/NT, and WA under its non-commercial ToU) are simply absent -> they hatch on the map, never paint 0.\"\n- web/src/@/lib/housing/highlight-metrics.ts:41 documents the wire contract \"crime percentile ranks (0 = no data; > 0 always when covered)\"; lines 289/296/303 gate only on rank > 0, and there is NO state-level availability gate - the three crime metrics are always offered in \"Colour by\".\n- crimeRankScale() (highlight-metrics.ts:406) at v=0.1 yields interpolateYlOrRd(0.1508), a real pale-yellow fill, not the no-data hatch. So every non-NSW suburb paints as \"covered, lowest crime percentile\".\n- Full branch diff grep for \"crime\" touches only the 000107 MV-refresh hardening; the web layer is untouched by this branch. GetSuburbProfile is unaffected (house_prices.go:278 reads c.PctRank from crime rows), but ListStateSuburbs (house_prices.go:133) passes the store value straight to the proto.\n\nSeverity blocker rather than major because the regression flips the failure direction from safe to unsafe: pre-change, a covered-lowest suburb was mistaken for no-data and hatched (under-publish); post-change, no-data is mistaken for covered and a fabricated national crime percentile is published for ~every suburb outside the loaded jurisdiction - including precisely the small_pop/unreliable rows migration 000092 exists to withhold. That violates the withhold-rather-than-guess editorial standard on a public surface.\n\nTest lock-in confirmed: postgres_house_prices_query_test.go:44 TestListStateSuburbsQuery_SeparatesCoveredLowRanksFromNoData asserts the buggy literal string for all three ranks, so the defect is pinned by a test asserting the opposite of the SQL's behaviour.\n\nOne nuance on the reviewer's wording (does not change the verdict): the expression DOES correctly floor genuinely covered low ranks (0.006 -> 0.1); it just floors NULL as well, so the claimed separation never happens. The proposed fix (CASE WHEN cr.<rank> IS NULL THEN 0 ELSE GREATEST(ROUND(cr.<rank>::numeric, 1), 0.1) END, plus rewriting the test to assert 0-for-NULL behaviourally rather than matching the literal) is correct. Nothing was modified during verification."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "blocker",
    "file": "services/migrations/000109_fix_listing_rollup_correctness.up.sql",
    "line": "277",
    "title": "NULLing mv_agency_stats.dropped_count below k=3 inverts the agency board's default ranking (ORDER BY \u2026 DESC is NULLS FIRST)",
    "detail": "`CASE WHEN COUNT(*) >= 3 THEN COUNT(*) END AS dropped_count` replaces 000086's `COUNT(*) AS dropped_count` + `COALESCE(da.dropped_count, 0)` in the final SELECT (line 298 also drops the COALESCE). The MV column is now NULL both for agencies with 1\u20132 dropped addresses and \u2014 via the LEFT JOIN at line 300 \u2014 for every agency with no drops at all.\n\nListAgencyPriceStats (postgres_house_prices.go:1316) sorts with `dropped_count DESC, total_drop_value DESC, active_listings DESC`. Those unqualified names resolve to the MV's *input* columns (the output columns are named `coalesce`), and Postgres defaults DESC to NULLS FIRST. The `sort=value` branch (line 1323, `total_drop_value DESC`) has the identical problem; only `sort=avg_cut` (line 1321) spells NULLS LAST.\n\nReproduced against the real migrations on PG 17.6 with two seeded agencies (5 active addresses each; one with 5 dropped addresses, one with none), running the exact store query:\n\n    agency_id | agency_name     | dropped_count | total_drop_value\n    AG-QUIET  | Quiet Realty    |             0 |                0   <-- rank 1\n    AG-BIG    | Big Cuts Realty |             5 |           250000   <-- rank 2\n\nRolling 000109 back and re-running the same query restores the correct order (Big Cuts first), confirming it is a regression, not pre-existing.\n\nNote also that suppressing a *count* is outside F07's stated scope (\"suppress proprietary asking/sold/drop prices below three addresses\") \u2014 000086 already suppressed only depth (avg_drop_pct) and value.",
    "failure_scenario": "/price-drops calls listAgencyPriceStats(\"\", \"drops\", 12) (web/src/app/price-drops/page.tsx:76). Since almost every agency has fewer than 3 dropped addresses in a 30-day window, the ORDER BY puts hundreds of NULL-dropped_count agencies ahead of the real cutters and LIMIT 12 truncates before reaching any of them. The section headed \"agencies ranked by asking-price cuts\" (agency-drops-board.tsx:40) renders twelve rows each reading \"0 cuts\", and the agency actually cutting the most prices never appears.",
    "fix": "Keep `COUNT(*) AS dropped_count` unsuppressed (or restore `COALESCE(da.dropped_count, 0)` in the final SELECT) and suppress only avg_drop_pct/total_drop_value as 000086 did; if the count must stay NULLable, add explicit `NULLS LAST` to every DESC key in the store's orderBy whitelist."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "blocker",
    "note": "Reproduced end-to-end on PG 17.6; every refutation avenue failed.\n\nCODE FACTS VERIFIED\n- 000109_fix_listing_rollup_correctness.up.sql:277 is verbatim `CASE WHEN COUNT(*) >= 3 THEN COUNT(*) END AS dropped_count`; the final SELECT (~line 298) emits bare `da.dropped_count` / `da.total_drop_value` over `LEFT JOIN da USING (source, agency_id, state_code)`. So both are NULL for 1-2-drop agencies AND for every zero-drop agency. 000086 emitted `COALESCE(da.dropped_count,0)` / `COALESCE(da.total_drop_value,0)` (NOT NULL).\n- postgres_house_prices.go:1316 default orderBy = `dropped_count DESC, total_drop_value DESC, active_listings DESC`. The SELECT list wraps both in COALESCE WITHOUT aliases, so the output column names are literally `coalesce`; the ORDER BY simple names bind to the MV input columns (nullable) and DESC defaults to NULLS FIRST. Only the `avg_cut` branch (line 1321) spells NULLS LAST; `value` (line 1323) has the identical defect.\n\nREPRODUCTION (throwaway local DB `mvverify`, since dropped; no repo files touched \u2014 `git status` clean)\nMinimal property_listings/property_price_events fixture + VERBATIM 000109 agency-MV DDL + VERBATIM store query:\n  AG-QUIET (0 drops)  -> rank 1, renders \"0 cuts\"\n  AG-TWO   (2 drops)  -> rank 2, renders \"0 cuts\"\n  AG-BIG   (5 drops, $250k) -> rank 3\nRolling the MV back to the VERBATIM 000086 DDL and re-running the same query restores AG-BIG -> rank 1. `sort=value` inverts identically. 000109 .down.sql:206,208 restores the COALESCEs, confirming the up migration is the regression.\n\nREFUTATIONS ATTEMPTED AND FAILED\n- Handler re-sort: none. house_prices.go:676-720 maps rows 1:1 in DB order and caches the response.\n- Frontend filter: none. price-drops/agency-drops-board.tsx:12 filters only on agencyName; line 40 renders `{a.droppedCount} cut{...}` -> twelve \"0 cuts\" rows.\n- Kill switch: dropListingsEnabled() (house_prices.go:408) is ON by default (only explicit false/0/off/no disables).\n- Guard in diff: none. Worse \u2014 mv_correctness.test.mjs explicitly pins `CASE WHEN COUNT\\(\\*\\) >= 3 THEN COUNT\\(\\*\\) END AS dropped_count`, locking the regression in.\n- Unreachable path: no. price-drops/page.tsx:77 calls listAgencyPriceStats(\"\", \"drops\", 12) and the section renders whenever the list is non-empty, which NULLS FIRST guarantees.\n\nSCOPE NOTE UPHELD: suppressing a *count* exceeds the k-anon scope 000086 applied (depth/value only).\n\nONLY UNCERTAINTY (does not affect reachability): the exact number of stolen slots depends on prod agency distribution. Irrelevant to the verdict \u2014 any zero-drop agency with >=3 active listings gets NULL and outranks every real cutter, and such agencies are certain in a ~12k-listing corpus. Among NULL rows the tiebreak is active_listings DESC, so the board fills with the largest non-cutting agencies.\n\nSEVERITY: blocker retained \u2014 a flagship public page section titled \"Agencies cutting hardest\" would ship rendering twelve agencies each reading \"0 cuts\", with the actual top cutters absent. Fix is trivial (restore COALESCE in the MV or add NULLS LAST to every DESC key in the store whitelist) and the accompanying test must be updated too."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "major",
    "file": "services/migrations/000109_fix_listing_rollup_correctness.up.sql",
    "line": "34-40",
    "title": "12-month sold window keys off first_seen/status_change only, silently dropping every sale recorded as a `relisted` event",
    "detail": "`sold_transitions` (lines 34-40, duplicated for the state MV at 191-197) requires `e.event_type IN ('first_seen', 'status_change') AND e.listing_status = 'sold'`. But crawl_listings_diff.go:252-268 explicitly *suppresses* status_change when the listing was inactive:\n\n    if !prev.IsActive { ... e.EventType = \"relisted\"; relisted = true }\n    // status_change (suppressed when relisted already conveys the transition).\n    if !relisted && l.Status != \"\" && prev.Status != l.Status { ... }\n\nDelisting sets `is_active = false, listing_status = 'withdrawn'` (crawl_listings_store.go:260), so any listing that drops off the /buy/ or /sale/ SRP long enough to be delisted and later reappears tagged Sold produces a `relisted` event carrying listing_status='sold' \u2014 and only that event. property_listings.listing_status is 'sold', but no first_seen/status_change 'sold' event exists, so the address is invisible to the new CTE. The previous definition (000077) read `property_listings.listing_status = 'sold'` directly and captured it.\n\nReproduced on PG 17.6 with 3 sold listings whose only sold-marking event is `relisted`:\n\n    OLD | SUBURB:NSW-2026-BONDI | sold_count=3 | avg_sold=1500002\n    NEW | SUBURB:NSW-2026-BONDI | sold_count=0 | avg_sold=NULL\n    STATE-NEW | NSW | sold_count=0 | avg_sold=NULL",
    "failure_scenario": "A Domain listing in Bondi is delisted after the grace period (status\u2192'withdrawn', is_active=false), then reappears on the /sale/ SRP tagged Sold a fortnight later. eventsFor() emits `relisted` with listing_status='sold' and no status_change. mv_suburb_listing_stats.sold_count and mv_state_price_drops.sold_count both omit it, so the /price-drops suburb leaderboard and state board render \"\u2014\" for median sold price on suburbs that did have qualifying sales (memory records ~2,574 Domain delistings, i.e. a real population of relist paths).",
    "fix": "Either add 'relisted' to the event_type IN list, or derive sold_at from `MAX(observed_at) FILTER (WHERE listing_status = 'sold')` over all event types for the listing."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "major",
    "note": "Verified against the worktree and reproduced on an ephemeral PG 17.6 cluster (torn down afterwards; nothing modified).\n\nSQL as cited: services/migrations/000109_fix_listing_rollup_correctness.up.sql:34-40 (suburb MV) and :191-197 (state MV) both gate sold_transitions on `e.event_type IN ('first_seen','status_change') AND e.listing_status='sold'`. `relisted` is a declared event type (000076_add_property_listings.up.sql:61) and cannot match.\n\nPipeline really emits relisted-only sold markers: crawl_listings_diff.go:252-270 emits `relisted` whenever `!prev.IsActive`, carrying `base.Status` (the current card's status), and status_change is guarded by `if !relisted && ...` so the two are mutually exclusive. insertPriceEvent (crawl_listings_store.go ~220) persists that Status into listing_status. markAbsent (crawl_listings_store.go:259-262) sets is_active=false, listing_status='withdrawn' after delistGrace (default 2, crawl_listings.go:111). A later sweep seeing the same (source,listing_id) tagged Sold (crawl_listings_extract.go:357-362, Domain UPVSoldListings tags) upserts listing_status='sold' while writing only a `relisted` event. The existing unit test crawl_listings_test.go:346-350 already exercises prev={Status:\"withdrawn\", IsActive:false} -> relisted.\n\nNo guard elsewhere: the new services/migrations/mv_correctness.test.mjs asserts the literal `event_type IN \\('first_seen', 'status_change'\\)`, pinning the omission; no backfill of synthetic sold events exists in the diff.\n\nRegression, not pre-existing: 000077_add_suburb_listing_stats.up.sql and 000086_price_drops_rollups.up.sql:135-140 both counted property_listings.listing_status='sold' directly with no event join.\n\nReachable in the read path: postgres_house_prices.go:706 and :1258 select sold_count/avg_sold/median_sold; web/src/@/components/housing/price-drops/state-drops-board.tsx:79 and suburb-drops-leaderboard.tsx:66 render \"\u2014\" at 0.\n\nReproduction (3 Bondi listings, trail first_seen(for_sale) -> delisted(withdrawn) -> relisted(sold)): OLD 000077 => 3 / 1500002; NEW 000109 => zero rows; adding 'relisted' to the IN list => 3 / 1500002. Matches the reviewer's numbers exactly.\n\nCorrections to the finding: (1) the magnitude claim is the soft part \u2014 the dominant sale path (sold seen while the row is still active) yields a status_change and IS captured, so this is not a wholesale loss of sold stats; the affected subset is only delisted(withdrawn)-then-reappeared-as-Sold under the same (source,listing_id), and I could not size it (local dev PG on :5438 is down, prod not queried). (2) the \"~2,574 Domain delistings\" figure is stale \u2014 crawl_listings.go:738 records 5,122 delistings across 33,351 Domain listings vs 205 across 44,630 REA.\n\nSeverity stays major: silent one-directional undercount on a public flagship board, amplified by the new k>=3 suppression (losing one sale can flip a suburb from a real median to \"\u2014\"), regressing shipped behaviour, with a one-token fix. Note the alternative fix (MAX(observed_at) FILTER (WHERE listing_status='sold')) makes sold_at the relist-observation date rather than the true sale date \u2014 fine, but worth documenting."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "major",
    "file": "services/migrations/000109_fix_listing_rollup_correctness.up.sql",
    "line": "293",
    "title": "Dropping mv_agency_stats.agent_names breaks the currently-deployed API under the repo's documented DDL-before-merge rollout order",
    "detail": "The rebuilt mv_agency_stats final SELECT (lines 293-303) no longer emits `agent_names`, and `dropped_count` is now NULLable. The store was updated in the same commit to select `'{}'::text[] AS agent_names` and `COALESCE(dropped_count, 0)` \u2014 which is forward- and backward-compatible \u2014 but the *deployed* build still runs the base-commit query (postgres_house_prices.go:1325-1326 at 8c120a352):\n\n    suburbs_covered, dropped_count, COALESCE(avg_drop_pct, 0),\n    COALESCE(total_drop_value, 0), COALESCE(agent_names, '{}')\n\nVerified against the rebuilt MV on PG 17.6:\n\n    ERROR:  column \"agent_names\" does not exist\n    HINT:  Perhaps you meant to reference the column \"mv_agency_stats.agency_name\".\n\nand independently, `dropped_count` NULL fails the old non-pointer int32 scan. CLAUDE.md mandates applying migrations by hand BEFORE merging (\"the prod deploy does NOT run migrate up\"), which guarantees this window. Note the same file already establishes the compat idiom for exactly this hazard \u2014 `COALESCE((to_jsonb(d) ->> 'dropped_value')::float8, 0)` at postgres_house_prices.go:707 exists so a code deploy ahead of DDL can't break the suburb board.",
    "failure_scenario": "Operator applies 000109 to prod Supabase on the session pooler (the documented pre-merge step), then merges. Until the Cloud Run revision with the new store code is live, every ListAgencyPriceStats call fails with `column \"agent_names\" does not exist`; getHousing.ts's withRetryAndNotFound swallows it to undefined and /price-drops renders with the entire \"Agencies\" section missing (and the KV entry is skipped, so it re-fails on every regeneration).",
    "fix": "Keep an `'{}'::text[] AS agent_names` placeholder column in the rebuilt MV (or land the code deploy first and apply the DDL after), and keep dropped_count non-NULL."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "major",
    "note": "Every claim verified; no guard exists anywhere in the diff.\n\nSCHEMA: 000109_fix_listing_rollup_correctness.up.sql:244 does `DROP MATERIALIZED VIEW IF EXISTS mv_agency_stats` and the rebuilt final SELECT (293-303) emits no `agent_names`; the prior definition (000086_price_drops_rollups.up.sql:234) did emit `COALESCE(agents.agent_names,'{}') AS agent_names` and emitted `COALESCE(da.dropped_count,0)` (non-NULL). The new `da` CTE is `CASE WHEN COUNT(*)>=3 THEN COUNT(*) END` + LEFT JOIN, so `dropped_count` is now NULLable. Removal is intentional and test-enforced (mv_correctness.test.mjs:87).\n\nDEPLOYED CODE BREAKS: at 8c120a352 the store selects `suburbs_covered, dropped_count, COALESCE(avg_drop_pct,0), COALESCE(total_drop_value,0), COALESCE(agent_names,'{}')` FROM mv_agency_stats and scans dropped_count into a non-pointer int32. Against the rebuilt MV that is an unavoidable 42703 at parse time (only mv_agency_stats is in FROM, so `agent_names` cannot resolve elsewhere); the NULL dropped_count scan is a second independent break.\n\nWINDOW IS PROCESS-GUARANTEED: .github/workflows/terraform-deploy.yml:1022-1030 applies a hardcoded prod allowlist ending at 000085 \u2014 000109 is not in it. CLAUDE.md mandates hand-applying migrations BEFORE merging; the branch commit message says only \"Migrations 000107-000109 (hand-apply on prod, session pooler 5432)\" with no ordering caveat. DDL applied at PR-open time can precede the deploy by longer than the 24h KV TTL (PRICE_DROPS_TTL=86400), and the API cache is per-instance MemoryCache on a min_instance_count=0 service, so cold instances hit the DB immediately.\n\nREACHABLE END TO END: dropListingsEnabled() (house_prices.go:408) defaults true (only false/0/off/no disables) and no Terraform sets HOUSING_DROP_LISTINGS_ENABLED, so the handler reaches the store; withRetryAndNotFound (web/src/app/actions/withRetry.ts:96-124) logs and returns undefined; price-drops/page.tsx:190 gates the section on `agencies?.agencies?.length > 0`, so the Agencies board silently vanishes exactly as described, and public /v1/listAgencyPriceStats consumers get Internal errors.\n\nREFUTATION ATTEMPTS FAILED: no placeholder/compat shim in 000109; 000107 only touches the refresh function; the other rebuilt MVs preserve every column the base-commit queries read (base GetPriceDropsOverview already COALESCEs the newly-nullable state columns), so agent_names is the ONLY dropped column \u2014 the finding's scoping is exact. Note the new store code is bidirectionally compatible ('{}'::text[] literal + COALESCE(dropped_count,0)), so code-first-then-DDL is entirely safe; the defect is that the diff silently inverts the repo's documented order without saying so.\n\nONE CORRECTION to the reviewer's supporting argument: the to_jsonb idiom at postgres_house_prices.go:707 was written for the MIRROR direction \u2014 its comment says \"dropped_value only exists once migration 000083 has rebuilt mv_suburb_price_drops... so a code deploy ahead of the manual prod DDL can't break the suburb board\". The technique covers removal too, so the precedent holds, but it shows the author assumed code-first, which is the opposite of the CLAUDE.md default. This sharpens rather than weakens the finding.\n\nSEVERITY: major stands. Real hard error on a live prod read path + public API endpoint, high probability under the written rollout doctrine, potentially >24h duration; mitigated only by graceful degradation and a flushable cache; one-line fix.\n\nADJACENT BUG (separate finding, not this one): the new store COALESCEs dropped_count in the SELECT list but leaves the raw NULLable column in ORDER BY (postgres_house_prices.go:1316/1319/1321/1323). Postgres defaults DESC to NULLS FIRST, so the default \"drops\" sort now ranks sub-k=3 agencies with NO reported cuts at the TOP of the board. Line 1321 already uses explicit NULLS LAST for avg_drop_pct, so this is an oversight, not a choice."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "minor",
    "file": "services/jobs/internal/jobs/houseprices/store.go",
    "line": "126",
    "title": "F05's caller-side timeout fix applied to only one of two byte-identical collector copies",
    "detail": "`git diff` of the two files at the base commit shows they were byte-identical; after this change they diverge. services/house-price-collector/store.go:126-130 now issues `SET statement_timeout = 0; SELECT refresh_housing_materialized_views()` (verified on PG 17.6 that this multi-statement simple query does disarm an already-armed session timeout, where the bare call does not), but services/jobs/internal/jobs/houseprices/store.go:126 still issues the bare `pool.Exec(ctx, \"SELECT refresh_housing_materialized_views()\")`. houseprices.Job() is registered in the consolidated binary (services/jobs/cmd/shorted/main.go:47) and the shorted-jobs image is built by terraform-deploy.yml:286-288, so this path is live code, merely not yet wired to a Cloud Run job for housing.",
    "failure_scenario": "When the housing collector is migrated onto the approved consolidated `shorted houseprices` binary (memory: jobs-consolidation is APPROVED), the refresh again runs under Supabase's role-level statement_timeout with only the function-scoped GUC \u2014 which 000095's own header documents as insufficient \u2014 reproducing the partial-refresh starvation 000107 exists to prevent.",
    "fix": "Apply the same two-statement Exec to services/jobs/internal/jobs/houseprices/store.go:126."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "Divergence verified. services/house-price-collector/store.go:125-132 now issues `SET statement_timeout = 0; SELECT refresh_housing_materialized_views()`; the clone at services/jobs/internal/jobs/houseprices/store.go:125-128 still issues the bare `pool.Exec(ctx, \"SELECT refresh_housing_materialized_views()\")`. Both files set `DefaultQueryExecMode = pgx.QueryExecModeSimpleProtocol` (line 40 of each), so the same two-statement fix applies verbatim.\n\nReviewer detail corrected (immaterial): the two files were NOT byte-identical at 8c120a352 \u2014 md5s differ and `diff -u` shows exactly ONE differing line across 508 lines each (`package main` vs `package houseprices`). Line-for-line clones otherwise, so the claim's substance survives.\n\nRegistration/build claims check out: houseprices.Job() at services/jobs/cmd/shorted/main.go:47; shorted-jobs image built at .github/workflows/terraform-deploy.yml:286-288.\n\nReachability is future/manual, not scheduled \u2014 which is why minor is the right grade, and the finding says so itself. Today the deployed housing job uses the STANDALONE image (terraform/modules/house-price-collector + var.house_price_collector_image, still built at terraform-deploy.yml:283-284), and the residential rigs run `$HOME/bin/house-price-collector` (deploy/run-housing-crawl.sh:49) built from `./house-price-collector/` (deploy/README.md:11). docs/jobs-consolidation-plan.md:58 lists house-prices as \"migrate last\"; job_test.go:15 states the rig contract in the future tense. It IS manually reachable now (`shorted house-prices` with a prod DATABASE_URL), and three call sites lose the fix at cutover: job.go:416, crawl_listings.go:353, crawl_agent.go:688.\n\nPremise validated: 000107's own trailing comment says the ALTER FUNCTION GUC is \"belt-and-braces only: callers must disable an already-armed timeout\", consistent with PG's start_xact_command/enable_statement_timeout re-arming logic for the second statement of a simple-query batch. Proposed one-line fix is correct."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "blocker",
    "file": "services/shorts/internal/store/shorts/postgres_house_prices.go",
    "line": "302-304",
    "title": "F33 crime floor is inverted: GREATEST ignores NULL, so \"no crime data\" now publishes as rank 0.1 (safest) instead of the no-data hatch",
    "detail": "`COALESCE(GREATEST(ROUND(cr.break_ins_rank::numeric, 1), 0.1), 0)` relies on GREATEST propagating NULL. PostgreSQL does the opposite: GREATEST/LEAST ignore NULLs and return NULL only if every argument is NULL, so `GREATEST(NULL, 0.1)` = 0.1 and the outer COALESCE(...,0) is dead code. The `cr` subquery (lines 260-271) is a LEFT JOIN over the gated `mv_suburb_crime_latest` (`WHERE NOT small_pop AND NOT unreliable`), so `break_ins_rank`/`violent_rank`/`motor_vehicle_rank` are NULL for (a) every suburb in a state with no crime ingest and (b) every NSW suburb deliberately quarantined by the small_pop/unreliable k-anon+reliability gate. Verified on postgres:16 with the real join shape: a suburb with no crime row returns `new_expr = 0.1` where the pre-change expression returned `0`. The frontend contract is `s.crimeBreakInsRank > 0 ? value : null` (web/src/@/lib/housing/highlight-metrics.ts:289,295,301), where null is the only way a suburb renders as no-data. So this change flips withheld/absent suburbs from \"no data\" to \"covered, lowest crime in the country\" on the choropleth and tooltip. The unit test added alongside it (postgres_house_prices_query_test.go:41-49) only asserts the literal SQL string, so it passes.",
    "failure_scenario": "Open /housing/VIC (or any state with no crime ingest) and pick Colour by \u2192 Break-ins. Every VIC suburb has no row in mv_suburb_crime_latest, so cr.break_ins_rank IS NULL \u2192 COALESCE(GREATEST(NULL,0.1),0) = 0.1 \u2192 highlight-metrics returns 0.1 instead of null \u2192 the whole state is painted at the safe end of the danger ramp and tooltips read \"0th pctile\" instead of showing the no-data hatch. Same for NSW suburbs excluded by the small_pop/unreliable gate: suppressed suburbs are now affirmatively labelled as the lowest-crime suburbs in Australia.",
    "fix": "Floor only the covered case, e.g. `CASE WHEN cr.break_ins_rank IS NULL THEN 0 ELSE GREATEST(ROUND(cr.break_ins_rank::numeric,1), 0.1) END`, and add a test that exercises the expression against a no-data row rather than string-matching the SQL."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "blocker",
    "note": "Reproduced on postgres:16.14 with the real join shape: with the gated `mv_suburb_crime_latest` LEFT JOIN (postgres_house_prices.go:260-271), a suburb with no crime row yields `COALESCE(GREATEST(ROUND(NULL::numeric,1),0.1),0) = 0.1` where the pre-change `COALESCE(ROUND(NULL,1),0)` yielded 0. GREATEST ignores NULLs (returns NULL only if every argument is NULL), so the outer COALESCE(...,0) is dead code. Verified `GREATEST(NULL::numeric, 0.1) = 0.1`.\n\nReachability confirmed, and no guard exists elsewhere in the diff (git diff --name-only shows no web/ files):\n- 000092_crime_read_gating.up.sql bakes `NOT small_pop AND NOT unreliable AND source_licence <> 'wa-tou-noncommercial'` into the MV, so quarantined NSW suburbs have NO row -> NULL after the LEFT JOIN.\n- services/house-price-collector/crime.go:63 ingests NSW only (`fetchNSW(ctx, registry[\"NSW\"])`), so 100% of suburbs in the other 7 states/territories are NULL.\n- Frontend contract unchanged: web/src/@/lib/housing/highlight-metrics.ts:289,296,303 = `rank > 0 ? rank : null`; choropleth-map.tsx:17 maps null -> `url(#nodata-hatch)`. state-suburb-map.tsx:150 renders ALL HIGHLIGHT_METRICS with no availability gating, so \"Colour by -> Break-ins\" is selectable in every state, and the metric's fixed `domain:[0,100]` means 0.1 paints at the safe end of crimeRankScale() instead of being renormalised.\n- highlight-metrics.ts:41 documents the contract being broken verbatim: \"crime percentile ranks (0 = no data; > 0 always when covered)\".\n- The new test postgres_house_prices_query_test.go:41-49 only strings.Contains-matches the SQL literal, so it passes regardless.\n\nIntent is legitimate (pct_rank = (\u03a3pop_below + 0.5\u00b7pop_u)/\u03a3pop\u00d7100 per crime_compute.go:330 is >0 but can round to 0.0 for a tiny lowest-rate suburb), but the implementation trades that rare case for misclassifying the dominant NULL case. GetSuburbProfile is unaffected \u2014 it populates ranks from the separate gated crime rows (house_prices.go:278-282).\n\nOne inaccuracy in the write-up that does NOT refute it: the map tooltip (suburb-tooltip.tsx) does not render the selected metric, so \"tooltips read 0th pctile\" is wrong; the misstatement surfaces in the choropleth fill only (the legend uses the fixed 0-100 domain and is unchanged).\n\nSeverity stays blocker: a silent regression that flips withheld/absent suburbs from the no-data hatch to an affirmative \"lowest crime in Australia\" claim across 7 states plus every k-anon-suppressed NSW suburb, contradicting the \"withhold rather than guess\" editorial standard; the proposed `CASE WHEN cr.<x>_rank IS NULL THEN 0 ELSE GREATEST(ROUND(...),0.1) END` is a one-line correction."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "blocker",
    "file": "services/shorts/internal/store/shorts/postgres_house_prices.go",
    "line": "1316",
    "title": "Agency board ranking inverted: mv_agency_stats.dropped_count/total_drop_value are now NULL below k=3 and the ORDER BY is `DESC` (NULLS FIRST)",
    "detail": "000109 changed `da` to emit `CASE WHEN COUNT(*) >= 3 THEN COUNT(*) END AS dropped_count` and the equivalent for `total_drop_value` (000109_fix_listing_rollup_correctness.up.sql:277-279, 298-300), and the outer SELECT no longer COALESCEs them (the pre-change MV had `COALESCE(da.dropped_count, 0)` / `COALESCE(da.total_drop_value, 0)`, see the .down.sql:206-208). So every agency with 0, 1 or 2 dropped addresses \u2014 i.e. almost all of them \u2014 now has NULL in those columns. ListAgencyPriceStats orders on the raw MV columns: default `dropped_count DESC, total_drop_value DESC, active_listings DESC` (line 1316) and `sort=value` \u2192 `total_drop_value DESC, dropped_count DESC` (line 1323). PostgreSQL's default for DESC is NULLS FIRST, so the suppressed agencies sort to the top. Note line 1321 already uses `avg_drop_pct DESC NULLS LAST` for the one column that was previously nullable \u2014 the two newly-nullable columns were not updated. Verified end-to-end on postgres:16 with the real MV: 'Small Agency' (1 dropped address, suppressed\u2192NULL) ranked above 'Big Discounters' (5 cuts, $500k).",
    "failure_scenario": "/price-drops calls listAgencyPriceStats(\"\", \"drops\", 12) (web/src/app/price-drops/page.tsx:77). With the new MV, the 12 rows returned are the 12 agencies with fewer than 3 dropped addresses; the store COALESCEs their NULLs to 0 on read, so AgencyDropsBoard renders a ranked list whose #1..#12 all read \"0 cuts\" (agency-drops-board.tsx:41), and the agencies that actually cut the most prices never appear on the board at all.",
    "fix": "Either restore COALESCE(...,0) in the MV's outer SELECT (keeping the depth suppression on avg_drop_pct/total_drop_value only), or add `NULLS LAST` to every DESC term in the ListAgencyPriceStats orderBy whitelist."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "blocker",
    "note": "CONFIRMED \u2014 reproduced end-to-end; the reviewer did not misread anything, and no guard exists elsewhere in the diff.\n\nCode facts (all in /Users/benebsworth/projects/.worktrees/shorted-hw-mv-correctness):\n\n1. `services/migrations/000109_fix_listing_rollup_correctness.up.sql` `da` CTE emits `CASE WHEN COUNT(*) >= 3 THEN COUNT(*) END AS dropped_count` and `CASE WHEN COUNT(*) >= 3 THEN SUM(total_abs) END AS total_drop_value`, and the outer SELECT projects them bare (`da.dropped_count, da.avg_drop_pct, da.total_drop_value`) over a `LEFT JOIN da`. So agencies with 0, 1 or 2 dropped addresses get NULL in both columns. The MV's only filter is `WHERE ag.active_listings >= 3`.\n\n2. This IS a regression, not pre-existing. The actual prior definition \u2014 `000086_price_drops_rollups.up.sql:231,233` (faithfully mirrored by 000109's own .down.sql:206,208) \u2014 used raw `COUNT(*) AS dropped_count` plus `COALESCE(da.dropped_count, 0)` / `COALESCE(da.total_drop_value, 0)`, i.e. both were NOT NULL. Only `avg_drop_pct` was nullable, which is exactly why `postgres_house_prices.go:1321` alone carries `NULLS LAST`. The sibling MV in the same migration (`mv_state_price_drops`) keeps `COALESCE(d.dropped_count, 0)` and suppresses only depth/value \u2014 the agency MV is the odd one out.\n\n3. `postgres_house_prices.go:1316` (`dropped_count DESC, total_drop_value DESC, active_listings DESC`) and `:1323` (`total_drop_value DESC, dropped_count DESC`) have no `NULLS LAST`; Postgres DESC defaults to NULLS FIRST. Nothing downstream re-sorts or filters: handler `house_prices.go:676` applies no drop-count filter, `dropListingsEnabled()` (`house_prices.go:408`) defaults ON (kill switch only), `/price-drops/page.tsx:77` calls `listAgencyPriceStats(\"\", \"drops\", 12)`, and `agency-drops-board.tsx` renders the array in order printing `{a.droppedCount} cut(s)`.\n\nEmpirical repro (ephemeral postgres:16-alpine, verbatim MV SQL extracted from the migration + verbatim store ORDER BY, seeded with Big Discounters 5 cuts/$500k, Mid Agency 4 cuts/$200k, Small Agency 1 cut, Zero Cuts Realty 0 cuts/50 listings):\n- New MV: `Zero Cuts Realty (0) \u2192 Small Agency (0) \u2192 Big Discounters (5, $500k) \u2192 Mid Agency (4)`.\n- Pre-change MV (down.sql): `Big Discounters (5) \u2192 Mid Agency (4) \u2192 Small Agency (1) \u2192 Zero Cuts Realty (0)`.\n`sort=value` is inverted the same way; `sort=listings` is affected only in its tie-break (minor).\n\nNo guard: no test asserts ordering. `services/migrations/mv_correctness.test.mjs` actually pins the new NULLing (`CASE WHEN COUNT(*) >= 3 THEN COUNT(*) END AS dropped_count`) with no companion ordering assertion, and `postgres_house_prices_query_test.go` only checks the agent_names removal.\n\nSeverity: keeping blocker. The flagship `/price-drops` agency board is rendered 100% wrong on a public page \u2014 ranks #1..#12 are the suppressed agencies (ordered by `active_listings DESC` within the NULL group), all displaying \"0 cuts\", and the agencies that actually cut the most never appear. It is a merge-blocking regression introduced by a migration whose stated purpose is correctness. It is at the low end of blocker (no crash, no data leak, no PII exposure \u2014 the k-anon intent itself is preserved). The proposed fix is right; restoring `COALESCE(da.dropped_count, 0)` / `COALESCE(da.total_drop_value, 0)` in the MV's outer SELECT (keeping suppression on `avg_drop_pct` only) matches both `mv_state_price_drops` in the same migration and the existing `NULLS LAST` conventions, and adding `NULLS LAST` to every DESC term at lines 1316/1319/1323 would be belt-and-braces."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "major",
    "file": "services/migrations/000109_fix_listing_rollup_correctness.up.sql",
    "line": "118-129",
    "title": "The k=3 floor on mv_suburb_price_drops is arithmetically invertible: count+avg+median+max+max_abs on one row reconstructs each individual listing's cut",
    "detail": "The suburb drop row publishes six statistics over the same n addresses: dropped_listing_count, avg_drop_pct, median_drop_pct, max_drop_pct, max_drop_abs and dropped_value (SUM of drop_abs). At the floor itself (n=3) that is an over-determined system: min = 3*avg \u2212 median \u2212 max recovers the third address exactly, and max_drop_abs / max_drop_pct recovers the prior asking price of the deepest-cut address whenever the same address holds both maxima. Verified on postgres:16 against the migration's own MV: three seeded addresses cut 20% / 10% / 5% produce n=3, avg=0.116667, median=0.1, max=0.2, max_abs=200000 \u2192 reconstructed min = 0.050000 (exact) and reconstructed prior ask = $1,000,000 (exact). Every one of those figures is proprietary-tos-restricted per-listing data. This is not covered by the HOUSING_DROP_LISTINGS_ENABLED kill switch: ListSuburbPriceDrops (services/shorts/internal/services/shorts/house_prices.go:367) and GetPriceDropsOverview (house_prices.go:632) have no flag gate, so after a takedown flips the switch off the per-address facts remain derivable from the \"anonymous aggregate\" board.",
    "failure_scenario": "An unauthenticated caller hits ListSuburbPriceDrops for a suburb whose row shows droppedListingCount=3. From avgDropPct, medianDropPct and maxDropPct it computes the exact discount on all three homes, and from maxDropAbs/maxDropPct the exact pre-cut asking price of one of them \u2014 with HOUSING_DROP_LISTINGS_ENABLED=false and the address board dark.",
    "fix": "Do not publish extremes (max_drop_pct / max_drop_abs) alongside count+mean+median on the same k-floored row; either drop the max_* columns, raise the floor for rows that carry extremes, or bucket/round the published statistics so the system is under-determined."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "MECHANISM CONFIRMED, SEVERITY DOWNGRADED major -> minor.\n\nReproduced exactly. I lifted lines 77-132 of services/migrations/000109_fix_listing_rollup_correctness.up.sql verbatim into a scratch postgres:16 DB with three seeded addresses cut 20/10/5%: the MV emits dropped_listing_count=3, avg_drop_pct=0.11666666666666668, median_drop_pct=0.1, max_drop_pct=0.2, max_drop_abs=200000, dropped_value=265000. 3*avg - median - max = 0.05000000000000004 (the third cut, exact) and max_drop_abs/max_drop_pct = 1,000,000 (the prior ask) - drop_pct is literally drop_abs/prior_price (services/house-price-collector/crawl_listings_diff.go:302-303). (Scratch DB dropped; worktree untouched.)\n\nReachability also confirmed: ListSuburbPriceDrops and GetPriceDropsOverview are VISIBILITY_PUBLIC (proto/shortedapi/shorts/v1alpha1/housing.proto:37,57); the four dropListingsEnabled() call sites (services/shorts/internal/services/shorts/house_prices.go:437,479,593,678) cover only the per-address/per-listing/agency surfaces, not these two; values flow through as un-rounded float64/double (postgres_house_prices.go:702-703, SuburbPriceDrop fields 6-9).\n\nWHY IT IS NOT MAJOR - three things the finding gets wrong or overstates:\n\n1. NOT INTRODUCED BY THIS DIFF, AND THE DIFF IS A NET TIGHTENING. The published column set and the `WHERE a.dropped_listing_count >= 3` floor for mv_suburb_price_drops are byte-identical to services/migrations/000086_price_drops_rollups.up.sql:51-80, live on prod since 2026-07-21. 000109 changes only the dedup unit (address_key-only), adds pl.is_active, and region-scopes the DISTINCT ON. Meanwhile the SAME migration ADDS a k=3 floor to mv_state_price_drops, which in 000086 (lines 146-149) published avg/median/max_drop_pct with NO floor at all - i.e. this change strictly improves the disclosure posture it is being faulted for.\n\n2. NO IDENTITY OR ADDRESS LINKAGE IS RECOVERABLE. The reconstruction yields an unattributed multiset of three cut percentages within a suburb. \"The per-address facts remain derivable\" is true only for anonymous values; nothing ties a figure to an address, owner or agent. The underlying asking prices are publicly advertised on REA/Domain - the restriction is licence/ToS republication, not personal data.\n\n3. THE PRIOR-ASK INVERSION IS CONDITIONAL AND UNVERIFIABLE BY THE CALLER. MAX(max_pct) and MAX(max_abs) are taken independently, both per-address (lines 92-93) and across addresses (lines 107-108), so max_abs/max_pct is a real prior price only when one address+event carries both maxima - the caller cannot tell. In real suburbs the deepest-percentage cut is frequently not the largest dollar cut.\n\nAlso note max_drop_abs is, by construction, one listing's exact dollar reduction at ANY n (proto comment: \"largest single reduction, AUD\") - so the extremes columns, not the k=3 floor, are the root, and they predate this change. The proposed fix (drop max_*, raise the floor for rows carrying extremes, or bucket the published stats) is a reasonable hardening item and the finding's arithmetic is sound - it just describes pre-existing shipped behaviour with no identity exposure and no regression, which is a minor follow-up rather than a blocker on this PR."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "minor",
    "file": "services/jobs/internal/jobs/houseprices/store.go",
    "line": "126",
    "title": "The consolidated `shorted` jobs binary keeps the pre-fix refresh call, so F05 silently un-fixes itself at the Phase-2 cutover",
    "detail": "services/house-price-collector/store.go:125-131 now sends `SET statement_timeout = 0; SELECT refresh_housing_materialized_views()` as one simple-protocol command (necessary because the ALTER FUNCTION ... SET statement_timeout in 000107 cannot disarm a timer already armed for the calling statement). The byte-duplicate of that file under the consolidated jobs binary \u2014 services/jobs/internal/jobs/houseprices/store.go:126 \u2014 still issues a bare `SELECT refresh_housing_materialized_views()`. Housing is currently the only job still on its own module/image (terraform/environments/prod/main.tf:185-192 uses var.house_price_collector_image, while announcements/economy/news/signals/weekly-report already run var.shorted_jobs_image), so the fix is live today, but nothing pins it there.",
    "failure_scenario": "When housing is switched to the `shorted houseprices` subcommand like the other five jobs, refresh_housing_materialized_views() runs again under the pooler's inherited statement_timeout; the first MV to exceed it raises query_canceled and the caller-side protection F05 added is gone (the in-function guards keep later views running, but the cancelled view stays stale silently \u2014 the exact 19-day staleness class that 000095 was written for).",
    "fix": "Apply the same `SET statement_timeout = 0;` prefix in services/jobs/internal/jobs/houseprices/store.go, or make one of the two files the single source."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "CONFIRMED (latent, not live). All claims verified in the worktree.\n\nEvidence:\n- Byte-duplicate confirmed at base: `git show 8c120a352:services/house-price-collector/store.go` and `8c120a352:services/jobs/internal/jobs/houseprices/store.go` are identical.\n- The branch diff touches exactly one Go file outside `services/shorts`: `/Users/benebsworth/projects/.worktrees/shorted-hw-mv-correctness/services/house-price-collector/store.go` (+5 -1). Nothing under `services/jobs/`.\n- `/Users/benebsworth/projects/.worktrees/shorted-hw-mv-correctness/services/jobs/internal/jobs/houseprices/store.go:126` still reads `_, err := pool.Exec(ctx, `SELECT refresh_housing_materialized_views()`)`, reachable from `job.go:416` (-mode refresh|all), `crawl_listings.go:353`, `crawl_agent.go:688`.\n- No compensating guard: `grep -rn statement_timeout services/jobs --include='*.go'` = 0 hits; its `connect()` uses the same QueryExecModeSimpleProtocol pool with no session GUC, so it inherits the pooler timeout. 000107's `ALTER FUNCTION ... SET statement_timeout TO '0'` cannot disarm a timer already armed for the calling statement (the stated reason the caller-side SET exists), so the migration does not cover this path.\n- The new test pins the fix to one file only: `services/migrations/mv_correctness.test.mjs:44-53` reads `../house-price-collector/store.go`. No byte-parity check exists between the two trees (`houseprices/job_test.go:83`'s \"parity guard\" only covers -mode dispatch coverage), so CI will not flag the drift.\n- Deployment claim accurate: `terraform/environments/prod/main.tf:185-192` uses `var.house_price_collector_image` while announcements/economy/news/signals/weekly-report use `var.shorted_jobs_image` (219/265/303/357/447/494); dev matches (`dev/main.tf:137`). Rigs also still exec the old binary: `services/house-price-collector/deploy/housing-crawl-common.sh:43` and `run-housing-crawl.sh:49` -> `BIN=\"${HOUSING_CRAWL_BIN:-$HOME/bin/house-price-collector}\"`. `services/jobs/README.md:49` + Phase-2d notes: port is \"CODE ONLY\", rig cutover deliberately deferred.\n\nRefutation attempts that failed: no parity/sync CI check; no DSN/pool-level timeout disarm in the jobs module; not dead code (the subcommand is registered at `services/jobs/cmd/shorted/main.go:29,47`, so `shorted house-prices -mode refresh` runs the unfixed path today for a hand-run).\n\nSeverity minor is right: zero deployed surfaces run the jobs copy today (Cloud Run job, rigs and the documented local runbook all exec `house-price-collector`), the only present-day exposure is a manual `shorted house-prices` run, and the consequence is a silently stale MV rather than corruption. It becomes a live regression at the Phase-2d cutover, which the README describes as a one-line HOUSING_CRAWL_BIN/args flip \u2014 a change nobody would expect to re-open this function. Fix as proposed (mirror the `SET statement_timeout = 0;` prefix in the jobs copy, and ideally extend mv_correctness.test.mjs to assert BOTH files so the duplicate cannot drift again).\n\nNit: the subcommand is spelled `house-prices` (package `houseprices`), not `shorted houseprices` as written in the finding \u2014 cosmetic only."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "blocker",
    "file": "services/shorts/internal/store/shorts/postgres_house_prices.go",
    "line": "302-304",
    "title": "F33 crime floor inverted: GREATEST ignores NULL, so \"no data\" now reports 0.1 instead of 0",
    "detail": "The new expression is `COALESCE(GREATEST(ROUND(cr.break_ins_rank::numeric, 1), 0.1), 0)` (same for violent/motor_vehicle). In PostgreSQL GREATEST/LEAST *ignore* NULL arguments and only return NULL when every argument is NULL, so `GREATEST(NULL, 0.1)` = 0.1 and the outer COALESCE(...,0) is dead code. `cr` is a LEFT JOIN over mv_suburb_crime_latest (line 259 `listStateSuburbsCrimeJoin`), so `cr.break_ins_rank` is NULL for every suburb with no crime row or one filtered by `NOT small_pop AND NOT unreliable`. I confirmed the semantics on postgres:16: `SELECT COALESCE(GREATEST(ROUND(NULL::numeric,1), 0.1), 0)` -> `0.1` (and `0.04` -> `0.1`, `0.72` -> `0.7`). The intended contract is documented in web/src/@/lib/housing/highlight-metrics.ts:41 (\"0 = no data; > 0 always when covered\") and enforced at lines 289/296/303 as `s.crimeBreakInsRank > 0 ? s.crimeBreakInsRank : null`, which drives the no-data hatch. Correct form: `CASE WHEN cr.break_ins_rank IS NULL THEN 0 ELSE GREATEST(ROUND(cr.break_ins_rank::numeric,1), 0.1) END`. Note the accompanying test (postgres_house_prices_query_test.go:44-52) only string-matches the buggy literal, so it locks the defect in rather than exercising it.",
    "failure_scenario": "Open /housing/VIC (or any state outside the NSW-only crime coverage) and pick Colour by -> Break-ins. ListStateSuburbs returns crimeBreakInsRank = 0.1 for all ~3,000 VIC suburbs instead of 0, `value()` returns 0.1 (not null), and the whole state is painted with the lowest-danger colour of the yellow->red ramp with a tooltip reading a fabricated \"0.1 percentile\". The no-data hatch becomes unreachable, and NSW suburbs deliberately withheld by the small_pop/unreliable gate are likewise shown as \"safest\" rather than withheld.",
    "fix": "Replace GREATEST with an explicit NULL test per rank column: `CASE WHEN cr.<rank> IS NULL THEN 0 ELSE GREATEST(ROUND(cr.<rank>::numeric, 1), 0.1) END`, and change the query test to assert the NULL branch (or add an integration case seeding an empty mv_suburb_crime_latest and asserting rank == 0)."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "blocker",
    "note": "CONFIRMED, blocker retained. Could not refute on any axis.\n\nCODE: /Users/benebsworth/projects/.worktrees/shorted-hw-mv-correctness/services/shorts/internal/store/shorts/postgres_house_prices.go:302-304 is exactly as cited, and `git diff 8c120a352` shows THIS commit (8652951b5) introduced the regression: `COALESCE(ROUND(cr.break_ins_rank::numeric, 1), 0)` -> `COALESCE(GREATEST(ROUND(cr.break_ins_rank::numeric, 1), 0.1), 0)` for all three ranks. The pre-change form was correct.\n\nSEMANTICS RE-VERIFIED independently on postgres:16.14 using the real join shape (listStateSuburbsCrimeJoin, line 259, LEFT JOIN over the gated mv_suburb_crime_latest):\n  NSW1 (covered 87.4) -> shipped 87.4 / proposed 87.4\n  NSW_SMALL (small_pop-gated) -> shipped 0.1 / proposed 0\n  VIC1 (no crime row) -> shipped 0.1 / proposed 0\nPG GREATEST ignores NULL args (NULL only when all args NULL), so the outer COALESCE(...,0) is dead code, exactly as claimed.\n\nREACHABILITY - no guard exists anywhere in the diff or downstream:\n- store scans raw into SuburbSummaryRow.CrimeBreakInsRank (line 351); service passes verbatim to proto (services/shorts/internal/services/shorts/house_prices.go:133). No remap. (GetSuburbProfile is a DIFFERENT path - builds ranks from p.Crime rows at house_prices.go:278 - so it is unaffected; only the list/map path is broken.)\n- web/src/@/lib/housing/highlight-metrics.ts:289/296/303 `s.crimeBreakInsRank > 0 ? ... : null` \u2014 0.1 > 0, so null is never returned.\n- choropleth-map.tsx:17 maps only null/undefined to url(#nodata-hatch) => hatch unreachable for these metrics.\n- state-suburb-map.tsx:150 renders HIGHLIGHT_METRICS unconditionally \u2014 NO per-state availability gating, so all three crime metrics are selectable on every state page including zero-coverage states.\n- Fixed domain [0,100] + crimeRankScale() => 0.1 paints the bottom of the yellow->red ramp, i.e. the \"safest\" colour, over ~every non-NSW suburb (crime is NSW-only Phase 1) and over every NSW suburb the small_pop/unreliable gate deliberately withholds.\n\nTEST: postgres_house_prices_query_test.go:46 string-matches the exact buggy literal, so it locks the defect in rather than exercising it. Confirmed.\n\nOnly inaccuracy is cosmetic: the tooltip renders `${Math.round(0.1)}th pctile` = \"0th pctile\", not \"0.1 percentile\". Does not affect the defect.\n\nSEVERITY: blocker. It is a regression introduced by this commit, it silently defeats the `NOT small_pop AND NOT unreliable` withholding gate that the same query's own comment calls \"defense-in-depth\", and it publishes a fabricated low-crime signal on a public surface with no flag \u2014 a direct violation of the repo's \"withhold rather than guess\" editorial rule.\n\nPROPOSED FIX as stated is correct: `CASE WHEN cr.<rank> IS NULL THEN 0 ELSE GREATEST(ROUND(cr.<rank>::numeric, 1), 0.1) END` per column, plus retargeting the query test at the NULL branch."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "blocker",
    "file": "services/shorts/internal/store/shorts/postgres_house_prices.go",
    "line": "1316",
    "title": "Agency board default ranking breaks: 000109 makes dropped_count/total_drop_value NULLable while ORDER BY still defaults to NULLS FIRST",
    "detail": "Migration 000109 changed the `da` CTE to `CASE WHEN COUNT(*) >= 3 THEN COUNT(*) END AS dropped_count` / `... THEN SUM(total_abs) END AS total_drop_value` (000109_fix_listing_rollup_correctness.up.sql:277-279) and dropped the outer `COALESCE(da.dropped_count, 0)` / `COALESCE(da.total_drop_value, 0)` that 000086 had (000086_price_drops_rollups.up.sql:231-233). Those columns are therefore NULL for every agency with 0, 1 or 2 dropped addresses - i.e. the overwhelming majority. The Go read path added `COALESCE(dropped_count, 0)` to the SELECT list (line 1330) but NOT to the ORDER BY: the default is `orderBy := \"dropped_count DESC, total_drop_value DESC, active_listings DESC\"` (line 1316) and `case \"value\"` is `\"total_drop_value DESC, dropped_count DESC\"` (line 1323). Postgres defaults DESC to NULLS FIRST, and `dropped_count` is not an output alias here (the SELECT list emits unaliased `coalesce` columns), so it binds to the NULLable MV column. The sibling `case \"avg_cut\"` already spells out `NULLS LAST`, which shows the hazard was known. I reproduced it on postgres:16 with a synthetic mv_agency_stats (2 agencies with 42 and 12 drops, 3 with NULL): the exact production query returned the three NULL agencies as rows 1-3 and never returned the top discounter.",
    "failure_scenario": "After 000109 is applied, GET /price-drops (server-rendered, sort defaults to \"drops\", limit 20 - web/src/app/actions/getHousing.ts:221) returns the 20 agencies with fewer than three dropped addresses. AgencyDropsBoard (web/src/@/components/housing/price-drops/agency-drops-board.tsx:39-40) renders each as \"0 cuts\" with no avg/value, and the agencies actually discounting most disappear from the board entirely. The result is then pinned into the Upstash `cache:housing:drops:*` entry for 24h.",
    "fix": "Order on the coalesced expressions (or add NULLS LAST): `COALESCE(dropped_count,0) DESC, COALESCE(total_drop_value,0) DESC, active_listings DESC` and the same for the \"value\" branch - or keep the MV columns non-NULL and suppress only avg_drop_pct/total_drop_value as the k-anon requirement (F07) actually asks."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "major",
    "note": "Reproduced end-to-end on postgres:16 using the migration's verbatim DDL; could not refute.\n\n(1) MV nullability is real: 000109_fix_listing_rollup_correctness.up.sql:275-303 computes `CASE WHEN COUNT(*) >= 3 THEN COUNT(*) END AS dropped_count` / `... THEN SUM(total_abs) END AS total_drop_value` in the `da` CTE and projects `da.dropped_count`/`da.total_drop_value` bare over a LEFT JOIN, dropping the `COALESCE(...,0)` that 000086:231/233 had. Ran the DDL verbatim against synthetic property_listings/property_price_events (6 agencies x 8 active listings; 5/4/2/0/0/0 dropped addresses): agencies with 0-2 drops returned NULL dropped_count and NULL total_drop_value.\n\n(2) ORDER BY binds to the NULLable MV column, not an output alias: the SELECT list emits `COALESCE(dropped_count, 0)` UNALIASED, so its output name is `coalesce` (confirmed in the psql header, and a temp view over the projection errors `column \"coalesce\" specified more than once`). `dropped_count` matches no output name -> resolves to the input column; DESC defaults to NULLS FIRST.\n\n(3) The exact production query text from postgres_house_prices.go:1325-1335 with the default orderBy (line 1316) and LIMIT 4 returned the four zero/low-drop agencies and never returned the 5-cut or 4-cut agency. The `case \"value\"` branch (line 1323) fails identically. The reviewer's proposed `COALESCE(dropped_count,0) DESC, COALESCE(total_drop_value,0) DESC, active_listings DESC` returned the correct top-2.\n\n(4) Reachable with no downstream guard: dropListingsEnabled() (house_prices.go:408) is ON by default; the handler coerces unknown sorts to \"drops\" and never re-sorts; web/src/app/price-drops/page.tsx:77 server-renders listAgencyPriceStats(\"\", \"drops\", 12) and only checks length>0 before rendering the \"Agencies cutting hardest\" section; agency-drops-board.tsx:39-40 prints `{droppedCount} cuts` in received order; getHousing.ts:238-241 pins the bad ordering into cache:housing:drops:* for PRICE_DROPS_TTL.\n\nCorroborating convention: the sibling suburb-drops query in the same file (line 680) already orders on `COALESCE(d.dropped_listing_count, 0)`, and the avg_cut branch (line 1321) already spells out NULLS LAST \u2014 the agency default/value branches are the outliers.\n\nTwo immaterial inaccuracies in the finding: the page passes limit 12 (20 is the action/handler default), and the reviewer cited getHousing.ts:221 for a limit that the page overrides.\n\nSeverity re-graded blocker -> major: no crash, no privacy/k-anon leak (suppression intent is preserved), and the state/suburb/address boards are unaffected \u2014 but one live public board publishes an inverted, meaningless ranking cached for 24h. The \"real discounters disappear entirely\" half needs >=12 agencies with <3 dropped addresses (near-certain in prod, but no prod DSN was available to count); the \"0-cut agencies rank #1\" half holds regardless of the prod distribution. Fix is one line and should land before merge.\n\nRead-only: worktree unmodified (git status clean); scratch docker container removed."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "minor",
    "file": "services/jobs/internal/jobs/houseprices/store.go",
    "line": "125-127",
    "title": "F05 caller-side timeout fix applied only to the legacy collector, not the ported jobs binary",
    "detail": "The fix lands in services/house-price-collector/store.go:125-131 (`SET statement_timeout = 0; SELECT refresh_housing_materialized_views()`), but the byte-identical port at services/jobs/internal/jobs/houseprices/store.go:125-127 still calls `pool.Exec(ctx, \"SELECT refresh_housing_materialized_views()\")`. services/jobs/README.md lists house-prices as ported (Phase 2d) and awaiting Terraform/rig cutover, so this copy is the intended future runtime. The new contract test (services/migrations/mv_correctness.test.mjs:44-52) reads only ../house-price-collector/store.go, so nothing flags the divergence. The migration's `ALTER FUNCTION ... SET statement_timeout TO '0'` cannot rescue it - as the fix's own comment says, a function-scoped GUC cannot disarm a timeout already armed for the calling statement.",
    "failure_scenario": "After the documented `shorted house-prices` cutover, a monthly ingest under Supabase's role-level statement_timeout re-hits the exact 000095/#349 failure mode: refresh_housing_materialized_views() is cancelled mid-run, the housing MVs (price drops, listing stats, headline, crime) silently go stale for weeks, and /price-drops + /housing serve frozen data while the job reports success.",
    "fix": "Apply the same two-statement Exec in services/jobs/internal/jobs/houseprices/store.go and extend mv_correctness.test.mjs to assert it in both files."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "Drift is real and verified: services/jobs/internal/jobs/houseprices/store.go:125-127 still runs bare `pool.Exec(ctx, \"SELECT refresh_housing_materialized_views()\")` while the fixed twin services/house-price-collector/store.go:125-131 prefixes `SET statement_timeout = 0;`. Both files configure `DefaultQueryExecMode = pgx.QueryExecModeSimpleProtocol` (line 40 in each), so the two-statement Exec ports over verbatim. services/migrations/mv_correctness.test.mjs:44-52 asserts only against ../house-price-collector/store.go (ran it: 4/4 pass), and grep over Makefile/services/Makefile/.github/workflows/package.json finds no runner for that .mjs at all, so nothing flags the divergence \u2014 the reviewer did not misread.\n\nTwo corrections that keep this at minor rather than higher:\n(1) Not reachable today. services/jobs/README.md:305-320 documents house-prices as \"CODE ONLY\" \u2014 Cloud Run job, residential rigs and local operator runs all still exec the standalone collector, and .github/workflows/terraform-deploy.yml:283 still builds services/house-price-collector/Dockerfile. The ported refreshHousingMV is a dormant duplicate until the cutover.\n(2) The stated failure scenario is overstated. Migration 000107 (same diff) is server-side, so it protects every caller including the port: each refresh sits inside `EXCEPTION WHEN query_canceled OR OTHERS` with a non-concurrent fallback, plus an outer per-view guard. statement_timeout is a one-shot timer armed at statement start and never re-armed once it fires, so after the cancel is trapped the fallback `REFRESH MATERIALIZED VIEW <mv>` and all later views run untimed. The 000095/#349 mode (one cancel starving every later MV for weeks) is exactly what 000107 removes. The residual harm from the missing caller-side SET is that the first view whose CONCURRENTLY refresh trips the timeout gets rebuilt with a blocking AccessExclusiveLock (stalling /price-drops and /housing readers), not silent multi-week staleness with a green job.\n\nThe PG reasoning in the finding is otherwise correct: a function-scoped `ALTER FUNCTION ... SET statement_timeout TO '0'` cannot disarm a timeout already armed for the calling statement (the migration itself calls it \"Belt-and-braces only\"), so the caller-side SET is the load-bearing half. Proposed fix stands: mirror the two-statement Exec in services/jobs/internal/jobs/houseprices/store.go and extend mv_correctness.test.mjs to assert both paths (and wire that test into a CI target, since today it runs nowhere)."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "minor",
    "file": "services/shorts/internal/store/shorts/postgres_suburb_explorer_test.go",
    "line": "204-208",
    "title": "The only behavioural test for F17 cannot execute - the integration test is already red before the assertion is reached",
    "detail": "The diff adds a `mv_register_suburb_property` stub to setupSuburbExplorerSchema (line 122) plus `assert.Equal(t, int32(2), p.Summary.PoliticianPropertyCount, ...)` at line 206. I ran `go test -tags=integration -run TestHousingLicenceGate ./internal/store/shorts/ -v`: TestHousingLicenceGate_SuburbProfile fails at line 204 with `ERROR: column d.banner_archetype does not exist (SQLSTATE 42703)` (the test schema was never updated for migration 000084), and TestHousingLicenceGate_StateSuburbs fails at line 170 with `ERROR: relation \"mv_suburb_crime_latest\" does not exist (SQLSTATE 42P01)`. Both are pre-existing gaps, but the consequence is that the new assertion never runs - `require.NoError` aborts first. The only other coverage for F17 is postgres_house_prices_query_test.go:54-64, which greps the source file for the literal SQL/scan strings and therefore cannot detect a column-order or join-shape error.",
    "failure_scenario": "A future edit that (say) inserts a column between `banner_bg_url` and `declared_property_count` in the SELECT list without moving `&p.Summary.PoliticianPropertyCount` in the Scan call passes both the string-match test and CI, and GetSuburbProfile then either scan-errors at runtime or reports the wrong value - the added integration assertion that would have caught it is unreachable.",
    "fix": "Add the banner_* columns to the test suburb_demographics DDL and a stub mv_suburb_crime_latest so these two integration tests actually run, or drop the inert assertion and cover F17 with a mock/handler-level test that exercises the real scan order."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "Reproduced verbatim. `go test -tags=integration -run TestHousingLicenceGate ./internal/store/shorts/ -v` in the worktree: TestHousingLicenceGate_SuburbProfile fails at postgres_suburb_explorer_test.go:204 (`require.NoError`) with `column d.banner_archetype does not exist (SQLSTATE 42703)`, and TestHousingLicenceGate_StateSuburbs fails at line 170 with `relation \"mv_suburb_crime_latest\" does not exist (SQLSTATE 42P01)`. The new assertion (actually lines 209-210, not 206) is therefore unreachable. Both gaps are pre-existing: at base 8c120a352 postgres_house_prices.go already selected d.banner_archetype (L413) and joined mv_suburb_crime_latest (L268) while the base test schema stubbed neither \u2014 so the author added the mv_register_suburb_property stub without ever running the test.\n\nRefutation attempts that failed: (a) no guard elsewhere in the diff adds banner_* columns or a crime-MV stub (test file diff is +9 lines total); (b) the other new test, services/shorts/internal/services/shorts/house_prices_test.go::TestGetSuburbProfile_MapsPoliticianPropertyCount, uses a gomock store, so it covers only row\u2192proto mapping and cannot detect a SELECT/Scan column-order regression \u2014 the reviewer missed it but it does not close the gap (their proposed \"mock/handler-level test that exercises the real scan order\" is self-contradictory); (c) postgres_house_prices_query_test.go:54-64 is pure strings.Contains, so inserting a column between banner_bg_url and declared_property_count passes it.\n\nProduction code is NOT defective: re-running both tests via `go test -overlay=` against a patched copy kept OUTSIDE the worktree (banner_* columns + mv_suburb_crime_latest stub added; worktree untouched) makes both PASS, including PoliticianPropertyCount == 2. So F17's SELECT/Scan pairing is correct today; the only defect is a dead assertion.\n\nSeverity remains minor, if anything milder than stated: these tests sit behind `-tags=integration` and no CI job runs them \u2014 .github/workflows/terraform-deploy.yml:1519 runs `go test ./...` untagged, and `make test-integration-ci` (services/Makefile:930) runs only ./test/integration/.... Zero production impact; it is a test-coverage/dead-weight issue. Fix as proposed: add banner_archetype/blurb/landmarks/bg_key/bg_url to the test suburb_demographics DDL plus an mv_suburb_crime_latest stub (that alone turns both tests green, verified)."
   }
  },
  {
   "target": "mv",
   "finding": {
    "severity": "minor",
    "file": "services/shorts/internal/store/shorts/postgres_house_prices.go",
    "line": "1331",
    "title": "Agency query is not migration-order tolerant: 000109 drops agent_names while the running API still selects it",
    "detail": "000109 recreates mv_agency_stats without the `agent_names` column (000109_fix_listing_rollup_correctness.up.sql:230-303). Per this repo's documented prod procedure, migrations are applied BY HAND before the code merges/deploys. The currently-deployed query (base commit) selects `COALESCE(agent_names, '{}')`, so during that window `ListAgencyPriceStats` fails with SQLSTATE 42703 and the handler converts it to `connect.CodeInternal` (house_prices.go:697-698 -> the error path at the end of the GetOrSet). The sibling suburb query at line 700 deliberately handles exactly this hazard with `COALESCE((to_jsonb(d) ->> 'dropped_value')::float8, 0)` (\"keeps this ONE query that works on both the old and new MV shape, so a code deploy ahead of the manual prod DDL can't break the suburb board\"); the agency query has no equivalent guard in either direction.",
    "failure_scenario": "Operator applies 000109 to prod Supabase on the session pooler before the new API image is promoted. Every ListAgencyPriceStats call 500s; web/src/app/actions/getHousing.ts's withRetryAndNotFound swallows it to `undefined`, AgencyDropsBoard returns null, and the agency section silently vanishes from /price-drops (or, worse, the stale 24h `cache:housing:drops:agency*` KV entry masks the outage) until the API rolls.",
    "fix": "Either keep an `agent_names text[]` column (empty array) in the rebuilt MV so both code versions work, or read it shape-agnostically like the suburb query does; alternatively document the strict deploy order (code first, then DDL) for this migration."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "Real and reachable. Verified in /Users/benebsworth/projects/.worktrees/shorted-hw-mv-correctness: (a) base 8c120a352 postgres_house_prices.go:1327 selects COALESCE(agent_names,'{}') from mv_agency_stats and scans it; (b) services/migrations/000109_fix_listing_rollup_correctness.up.sql:244-303 DROPs mv_agency_stats and recreates it without agent_names (the 000086 `agents` CTE is deleted; the down migration :199-209 restores it, so the column really is gone, not renamed); (c) the new query at :1331 uses a literal '{}'::text[], so the NEW code is shape-tolerant \u2014 the gap is only old-binary-vs-new-MV. Ops sequence is real: prod does NOT run migrate up (.github/workflows/terraform-deploy.yml:1019-1032 applies a hardcoded allowlist ending at 000085) and the branch commit message itself says \"Migrations 000107-000109 (hand-apply on prod, session pooler 5432)\"; repo practice is DB-before-code. Blast radius confirmed: dropListingsEnabled() (house_prices.go:408-415) defaults ON; MemoryCache.GetOrSet (internal/services/shorts/cache.go:104-109) does NOT cache errors, so every post-TTL request re-hits the failing query and 500s via connect.CodeInternal (house_prices.go:719-722); web withRetryAndNotFound -> undefined and price-drops/page.tsx:190 gates the section on length>0, so it silently vanishes. The cited precedent is genuine (postgres_house_prices.go:692-696 explicitly keeps the suburb query shape-agnostic for exactly this hazard). Adversarial checks that did not refute: every column GetPriceDropsOverview (base :1249-1258) and the suburb queries read still exists in the rebuilt state/suburb MVs, so agent_names is the ONLY dropped column a live query reads \u2014 the rest of /price-drops stays up; no other consumer of mv_agency_stats.agent_names exists (other hits read property_listings.agent_names, still present via 000079); the reverse order (code first) is safe. One imprecision in the finding: \"no equivalent guard in either direction\" is wrong \u2014 the new query IS tolerant of both shapes; the missing guard is on the MV side (a compat empty agent_names column) for the still-running old binary; fix #1 is the correct mitigation. Severity kept minor: deploy-bounded, self-healing, one flag-gated section rather than the page, no data loss or privacy exposure (the change removes PII)."
   }
  },
  {
   "target": "crawl",
   "finding": {
    "severity": "blocker",
    "file": "services/house-price-collector/crawl_listings.go",
    "line": "622-624 (`if metaOK && onTargetResults > 0 ... return finish(sweepComplete)`)",
    "title": "New thin-suburb \"exhausted\" verdict returns sweepComplete with an EMPTY listing set \u2192 delists the whole suburb",
    "detail": "The new gate returns `finish(sweepComplete)` at line 624, but `collected` is only populated by the merge loop at lines 646-654, which is *below* this early return. Every pre-existing `sweepComplete` path (duplicate page 641, empty page 641/643, capped-complete tail 682, the two `upgradeIfPageMetaConfirms` upgrades) is reached only after page 1 has already passed the `len(matched) >= minPerPage` gate, so before this diff the invariant `sweepComplete \u21d2 collected >= minPerPage` held. This branch is the first path that can return `complete` with zero listings, and `complete` is exactly the status that unlocks the delist pass in `crawl_listings_diff.go:101-132`: `seen` is empty, so `activeListingsForRegion(source, regionCode)` returns every live listing in the suburb, each gets `missed_sweeps+1` via `markAbsent`, and at `delistGrace` (default 2) each gets a `delisted` price event and `status='withdrawn'`. The listings that were actually rendered on page 1 are also never upserted (no `first_seen`, no price diff), so a new thin suburb never enters `property_listings` at all. Because those delist events count as `events>0`, `agentJobTerminal`/`agentJobOutcome` (crawl_agent.go:870-896) bank the job as \"succeeded\" \u2014 the corruption is silent. Note the intended outcome was already reachable without this branch: with `onTargetResults=2, pageSize=25` the walk is sized to `wantPages=1`, so deleting the early return lets the loop fall through to the capped-complete tail (line 682) and return `sweepComplete` *with* the 2 listings.",
    "failure_scenario": "REA page 1 for a thin suburb renders its 2 on-target listings with `listings_total:2, totalResultsCount:104, pageSize:25`. minPerPage=5, so the gate fires: `metaOK && 2>0 && 2<5 && 2<=2` \u2192 `finish(sweepComplete)` with `collected` empty. Verified by running the real sweep (overlay test against `reaPageWithMeta([]string{\"a\",\"b\"},\"2026\",104,2,25)`): `status=complete listings=0 pages=1 blocks=0`. Downstream, `diffSuburb` sees `sweep.status==sweepComplete` and `seen={}`, so on the first such run every active listing in that suburb/portal gets missed_sweeps=1 and on the second consecutive run each is written as `delisted`/`withdrawn` \u2014 including the two listings the crawler literally just read off the page. The suburb then stays permanently withdrawn because the same gate fires every subsequent run.",
    "fix": "Populate `collected` (run the merge loop over `matched`) before returning, or simply drop the early return and let the walk fall through to the existing capped-complete tail at line 682, which already yields `sweepComplete` with the listings intact for `wantPages < softCap`. Add an assertion in `finishSweep` (or a test) that `sweepComplete` never carries 0 listings when page 1 matched >0."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "blocker",
    "note": "CONFIRMED (blocker). Reproduced with a Go -overlay test against the real sweepSuburbSource in /Users/benebsworth/projects/.worktrees/shorted-hw-crawl-correctness (worktree unmodified; git status clean):\n\n  thin-exhausted (listings_total=2, totalResultsCount=104, pageSize=25): status=complete listings=0 pages=1 blocks=0\n  healthy        (listings_total=5, totalResultsCount=105, pageSize=25): status=complete listings=5 pages=1 blocks=0\n\nMechanism verified end to end:\n- services/house-price-collector/crawl_listings.go:622-624 returns finish(sweepComplete) from inside the `page == 1 && len(matched) < minPerPage` gate (line 616). The merge loop that fills `collected` is at 646-654, BELOW it, and finish -> finishSweep(collected, ...) (line 784) builds sweep.listings from that map, so it is empty.\n- The gate arithmetic guarantees the page DID render listings: onTargetResults > 0 && onTargetResults <= len(matched) implies len(matched) >= 1. So it reports \"whole suburb seen, zero listings\" for a page that just rendered 1-4 real on-target listings.\n- crawl_listings_diff.go:100-131: status==sweepComplete with empty `seen` -> activeListingsForRegion (crawl_listings_store.go:231, WHERE source=$1 AND region_code=$2 AND is_active) returns every live listing in the suburb; each gets missed_sweeps+1 and, at delistGrace (default 2, crawl_listings.go:111), a `delisted` price event + is_active=false/listing_status='withdrawn'. The listings just read off page 1 are never upserted, so they are withdrawn and stay withdrawn while the suburb remains thin; a brand-new thin suburb never enters property_listings at all.\n- Silent: agentJobTerminal (crawl_agent.go:869-882) returns \"succeeded\" for events=0/blockedSweeps=0/skippedRows=0 (run 1) and for events>0 (run 2, the delist events).\n- Reachable on deployed config: default CRAWL_LISTINGS_MIN_PER_PAGE=5; ~/.shorted-housing-crawl.env does not set it and the outage-modes runbook records that scheduled launchd runs do not either. Trigger band is onTargetResults in {1,2,3,4}.\n\nThree corrections to the finding, none of which rescue it:\n1. The reviewer's alternative fix is WRONG. Deleting only the early return does not fall through to the capped-complete tail (line 682) \u2014 the enclosing thin-page1 gate then hits `*blockCounter++; return finish(sweepBlocked)` at 626-628, i.e. the pre-diff behaviour this commit exists to fix. Only the first half of the proposed fix is valid: populate `collected` (run the merge over `matched`) or move the whole thin-page1 gate below lines 646-654 before returning sweepComplete.\n2. The kill switch does NOT cover this path: lc.cfg.legacyCompleteness is consulted only inside sawWholeSuburb (758-769); the new branch returns sweepComplete unconditionally, so CRAWL_LISTINGS_LEGACY_COMPLETENESS=1 cannot disable it.\n3. Run 1 writes zero events, not >0 (markAbsent emits an event only once grace is exhausted) \u2014 the job is still banked \"succeeded\", so the silence claim holds for both runs.\n\nTest-coverage note for the fix: the new TestSweep_PageClassificationMatrix (crawl_listings_test.go:551+) asserts only sw.status and blocks, never len(sw.listings) \u2014 which is why the empty-listing-set regression passed. Every pre-existing sweepComplete test (e.g. TestSweep_CompleteOnEmptyPage, line 484) does assert the count."
   }
  },
  {
   "target": "crawl",
   "finding": {
    "severity": "minor",
    "file": "services/house-price-collector/crawl_listings.go",
    "line": "603-615 vs 616-628",
    "title": "The new thin-suburb gate is unreachable for the dominant REA thin-suburb shape (page-1 broadening trips the poison gate first)",
    "detail": "The poison gate at line 603 (`mismatch > 0.30`) runs before the new thin-page-1 gate at 616. For a genuinely thin REA suburb, page 1 is a 25-row SRP containing the suburb's 2-3 on-target rows plus ~22 broadened surrounding-suburb rows \u2014 mismatch \u22480.92 \u2014 so `sweepPoisonVerdict(page=1, collected=0, minPerPage)` returns `sweepBlocked` (crawl_listings.go:845-850, early page \u21d2 blocked) and control never reaches the new `listings_total` check. F12's stated goal (\"classify thin suburbs from on-target page metadata rather than treating a small page-1 result as a Kasada block\") is therefore not achieved for the broadened case, even though `PageMeta.OnTargetResults` is present and would prove exhaustion.",
    "failure_scenario": "REA page 1 for Bondi returns 2 on-target listings (postcode 2026) + 23 broadened rows (postcode 9999) with `listings_total:2, pageSize:25`. Verified by running the real sweep: `status=blocked listings=0 pages=1 blocks=1` \u2014 identical to pre-diff behaviour, breaker still incremented, no delist safety gained. Only the narrow case where the portal returns *only* the thin on-target set (mismatch 0) reaches the new gate \u2014 which is the case that hits the blocker above.",
    "fix": "If PageMeta confirms `OnTargetResults <= len(matched)` on page 1, consult that evidence inside/before the poison gate (a page whose on-target count matches the portal's own on-target total is exhaustion, not page-1 poison) rather than after it."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "CONFIRMED, severity minor (unchanged). Code-path claim is exactly right and I reproduced the reviewer's result byte-for-byte.\n\nORDERING VERIFIED (/Users/benebsworth/projects/.worktrees/shorted-hw-crawl-correctness/services/house-price-collector/crawl_listings.go): poison gate `if mismatch > 0.30` at line 603; new thin-page-1 check at line 622 inside the `page == 1 && len(matched) < minPerPage` branch at 616. `sweepPoisonVerdict` (line 845) returns sweepPartial only for `page > 1 && collectedMatched >= minPerPage`, so on page 1 with `collected` empty it always returns sweepBlocked -> `finish(sweepBlocked)` at 614 and line 616 is unreachable. The whole crawl_listings.go delta is +9/-0, entirely inside that branch, so no guard elsewhere in the diff was missed.\n\nREPRODUCED (ran real sweepSuburbSource via `go test -overlay=` with a temp test file; worktree never modified, `git status` clean):\n  2 on-target + 23 broadened rows, listings_total:2 pageSize:25 -> status=blocked listings=0 pages=1 blocks=1  (identical to the reviewer's claim)\n  2 on-target + 0 off                                        -> status=complete listings=0 pages=1 blocks=0\n  3 on-target + 1 off (mismatch .25)                         -> status=complete listings=0 pages=1 blocks=0\n  2 on-target + 1 off (mismatch .33)                         -> status=blocked  listings=0 pages=1 blocks=1\nThe reachable window is even narrower than the finding says: a single off-target row beside 2 on-target rows already trips the poison gate.\n\nEMPIRICAL PREMISE HOLDS on the repo's own live data, not intuition: crawl_listings_extract.go:90-99 records the confirmed live REA shape (New Farm totalResultsCount~969 broadened vs listings_total==63 on-target), i.e. surrounding stock is in the SAME paginated stream, so at pageSize=25 a padded page 1 is unavoidable for a suburb with <5 on-target listings. Memory note housing-crawl-rea-truncation.md independently measured `stop-broadening` (this same mismatch>0.30 gate) as the dominant REA stop decision (285 of 1,694 REA page records). I could not find local trace.jsonl artefacts (none under ~/.shorted-housing-crawl, /Volumes/gamma-systems-2, or the repo) to count page-1 mismatch directly, so \"dominant\" rests on those two sources rather than a fresh tally - but the mechanism itself is not in doubt.\n\nNUANCE THAT SOFTENS BUT DOES NOT REFUTE: partitionByTarget (crawl_listings_validate.go:93-111) excludes same-postcode/different-suburb rows from the mismatch numerator (soft miss). A thin suburb padded mostly from same-postcode neighbours (Bondi 2026 / Bondi Beach 2026) would therefore reach the new gate. Narrow class; REA broadens by radius across many postcodes.\n\nSEVERITY: minor is correct. This is an effectiveness/near-dead-code claim, not a regression - pre-diff behaviour for the broadened shape was already `blocked`, and blocked delists nothing, so nothing gets worse. Its weight comes from the companion blocker the finding references: the only shape that DOES reach the new gate returns sweepComplete with listings=0 (confirmed in my run - `finish` closes over `collected`, which is populated at line ~655, after the early return at 624), so F12 currently fires only in the case that is unsafe."
   }
  },
  {
   "target": "crawl",
   "finding": {
    "severity": "minor",
    "file": "services/house-price-collector/crawl_brandbrain.go",
    "line": "148-190 (`brandbrainAggregateName`) + 322-350 (`brandbrainObservations`)",
    "title": "The aggregate projection collapses distinct median keys to 3 canonical names, so a historical/neighbouring median can be stored as this quarter's suburb median",
    "detail": "`brandbrainMediansPayload` walks every JSON blob on the page and emits `{name, value}` pairs, where `name` is derived purely from the key: any key containing `median`+`price` (and not `rent`) becomes `median_house_price` / `median_unit_price` / `median_sale_price`. The original key, the DOM position, and any surrounding suburb/period context are discarded; entries are deduped only by (name, value), so N distinct medians on the page arrive as N indistinguishable `median_house_price` entries with `SuburbHint` as the only context. `crawl_extract.go:12-17` and `robustMedian` (crawl_validate.go:60-77) document that a portal page carries *multiple* median candidates on purpose \u2014 the local legacy extractor defends against that by taking the median of survivors, but the brandbrain path uses `firstNonZero` (crawl_brandbrain.go:353) and stamps the result with `Period: currentQuarterEnd()`. Previously the LLM at least received the full page and could use structure to pick the right one; after this change no such signal exists. `validateMedian` only checks absolute bounds + a 0.15-8x capital band, which an adjacent-suburb or last-year median passes trivially. (Impact is contained because `-mode crawl` is opt-in and never scheduled \u2014 main.go:61-68.)",
    "failure_scenario": "A REA/Domain suburb page carries `medianPrice: 1,850,000` (current) and `medianSoldPriceLast12Months: 1,620,000` (historical) \u2014 or a `medianPrice` for a nearby-suburb insights module. Both normalize to `median_house_price` and are emitted as two separate `aggregate_fields` entries with no qualifier. The extractor returns whichever it saw first; `firstNonZero` takes it, `validateMedian` passes (in-band), and it is written to `house_prices` as the suburb's `median_price/house` for `currentQuarterEnd()` with source `brandbrain_rea`.",
    "fix": "Carry the original key (and any sibling suburb/period label) alongside the canonical name in `brandbrainAggregateField`, or refuse to emit a median field when more than one distinct value maps to the same canonical name."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "Real regression, correctly diagnosed, with two small citation errors and stronger evidence than the reviewer had.\n\nVERIFIED MECHANISM: brandbrainAggregateName (crawl_brandbrain.go:148-192) collapses by substring only. Ran the function standalone: medianHousePrice, previousMedianHousePrice, medianHousePrice12MonthsAgo, nearbySuburbMedianHousePrice and medianPriceHouse2019 all return \"median_house_price\". Dedup key is name+value (line 125), so N distinct page medians emit as N unlabelled entries; original key, DOM position, period and neighbouring-suburb context are all discarded. Downstream is as described: firstNonZero (line 303, used 355) takes the first, validateMedian only enforces $100k-$50M plus the 0.15-8x capital band (crawl_validate.go:41-55), Period is stamped currentQuarterEnd() unconditionally (line 340), and crossCheck logs only above 30% divergence so a 12% wrong-vintage median is silent.\n\nSTRONGER THAN CLAIMED: brandbrain's ExtractRealEstate runs a deterministic path-precise pass FIRST, which its own header calls authoritative for suburb medians \u2014 extractArgonautMedians reads REA at marketProfileBySlug.insights.medianPrice.buy.{house,unit}.allBed.yearly, extractDomainMedians picks the best-median propertyCategory row (~/projects/brandbrain/backend/internal/discovery/real_estate_structured.go:362-480). Both gate on strings.Contains(pageHTML,\"ArgonautExchange\") / \"propertyCategor\". The new projection strips those markers, so the authoritative pass returns zero and only the LLM-over-flat-list remains. Against Domain's real schema (medianSoldPrice per propertyCategory x bedroom slice) that list is a dozen-plus indistinguishable entries. No guard was added anywhere else in the diff (checked crawl.go and crawl_brandbrain_test.go).\n\nREVIEWER IMPRECISION (not fatal): the stated failure keys medianPrice and medianSoldPriceLast12Months both normalize to median_sale_price, NOT median_house_price \u2014 and median_sale_price is not a field on reaListing, so that exact pair only reaches median_price/house via the LLM's own guess. Keys containing \"house\" do collapse as described. firstNonZero is at line 303, not 353.\n\nSEVERITY minor stands (containment verified, not assumed): -mode crawl is excluded from \"all\" and never scheduled (main.go:60-68); dryRun defaults ON, writes only with explicit CRAWL_DRY_RUN=false (crawl.go:159); and every public read path filters source_licence <> 'proprietary-tos-restricted' (postgres_house_prices.go:104,324,431,552,604,616 and mv_housing_headline in migration 000054), so a wrong median is stored but never user-visible.\n\nADJACENT (separate finding, not this one): REA's median lives at ...allBed.yearly.display (key \"display\"), so the projection likely captures no REA median at all \u2014 the tier may now silently produce zero REA medians rather than a wrong one."
   }
  },
  {
   "target": "crawl",
   "finding": {
    "severity": "blocker",
    "file": "services/house-price-collector/crawl_listings.go",
    "line": "622-624",
    "title": "F12 thin-suburb branch returns sweepComplete with an EMPTY listing set, arming the delist pass against nothing",
    "detail": "The new `stop-thin-page1-exhausted` branch returns `finish(sweepComplete)` at line 624, but `collected` is not populated until line 647 (`for _, m := range matched { collected[m.ListingID] = m }`), which is *below* it in the loop body. So the branch hands back `suburbSweep{listings: [], status: sweepComplete}` \u2014 the page-1 listings it just proved were genuine are discarded.\n\nThat matters because `sweepComplete` is the ONLY status that unlocks the delist pass: `crawl_listings_diff.go:102` runs `activeListingsForRegion` and marks every active listing not in `seen` as absent, delisting it once `missed_sweeps >= delistGrace` (default 2, `crawl_listings.go:111`). With `sweep.listings` empty, `seen` is empty, so EVERY active listing for that (source, suburb) is missed \u2014 including the very listings that rendered on page 1, which were never upserted so their `last_seen_at`/`missed_sweeps` are never reset.\n\nThis is a new reachable state: every other `sweepComplete` path (`stop-duplicate-page`, `stop-empty-page`, loop-bound `upgradeIfPageMetaConfirms`) requires `page > 1`, i.e. page 1's listings are already in `collected`. Before this change the same branch returned `sweepBlocked`, and `diffSuburb` returns early on `sweepBlocked` (touch nothing), so dropping the listings was harmless.\n\nSecondary effect: `*blockCounter++` is skipped, so `blockedSweeps == 0` and `agentJobTerminal` (crawl_agent.go:870) banks the job as \"succeeded\" \u2014 the suburb is never re-queued, which is exactly the \"never-attempted job banked as succeeded\" silent-stoppage class this subsystem has been bitten by before.\n\nVerified by running the real sweep against the branch's own fixture (`go test -overlay`, no repo files touched):\n  lc.sweepSuburbSource(bondi, \"rea\", reaPageWithMeta([\"a\",\"b\"], \"2026\", 104, 2, 25))\n  -> status=complete listings=0 pages=1 blocks=0\n\nThe added test `TestSweep_PageClassificationMatrix` (crawl_listings_test.go:548+) asserts only `sw.status` and the block counter, never `len(sw.listings)`, so it passes with zero listings \u2014 unlike the neighbouring `TestSweep_CompleteOnEmptyPage`, which does assert `len(sw.listings) != 5`.",
    "failure_scenario": "REA reports listings_total=2 for a small suburb (e.g. a thin regional/inner-city SAL) and page 1 renders both on-target listings. minPerPage=5, so the outer thin-page test fires; metaOK && onTargetResults==2 && 2 < 5 && 2 <= len(matched)==2 -> the new branch returns sweepComplete with listings=[]. diffSuburb writes no listing rows (no first_seen/price events, last_seen_at not refreshed) and then runs the delist pass with an empty seen-set: all active REA rows for that region get missed_sweeps+1. After 2 consecutive runs (delistGrace=2) every one of them \u2014 including the 2 listings the crawler had just successfully rendered \u2014 gets markAbsent(delist=true) plus a 'delisted'/'withdrawn' price event. The job is reported to the brandbrain queue as 'succeeded' (blockedSweeps==0), so nothing re-crawls it.",
    "fix": "Move the `collected` population above the page-1 thin/exhausted branch (or populate `collected` from `matched` before returning), so `finish(sweepComplete)` carries the listings it just validated. Add `len(sw.listings)` assertions to the matrix test."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "blocker",
    "note": "CONFIRMED by reading + running. crawl_listings.go:617-624 returns finish(sweepComplete) before `collected` is populated (line 647), so finishSweep (line 784) yields {listings: [], status: complete}. Reproduced with the branch's own fixture via a temp-dir `go test -overlay` (no repo files touched): sweepSuburbSource(bondi,\"rea\",reaPageWithMeta([\"a\",\"b\"],\"2026\",104,2,25)) -> status=complete listings=0 pages=1 blocks=0; control with 5 on-target -> listings=5, so the loss is specific to this branch.\n\nReachability of the harm verified: diffSuburb (crawl_listings_diff.go:20) returns early only on sweepBlocked (line 21); with empty listings the upsert loop is skipped (no first_seen, last_seen_at/missed_sweeps never reset) and line 102 then runs activeListingsForRegion(source, region_code) (store line 231) marking every active row absent, delisting at missed >= delistGrace (default 2, crawl_listings.go:111) with a delisted/withdrawn price event.\n\nThe \"new state\" claim is correct: every other sweepComplete producer implies non-empty collected \u2014 stop-duplicate-page / stop-empty-page are page>1 gated; stop-broadening / stop-yield-decay go through sweepPoisonVerdict (line 845, requires collectedMatched >= minPerPage) or page>1; the loop-bound return needs page 1 to have passed len(matched) >= minPerPage; outcomeBlocked/outcomeError/stop-stub use blockedOrPartial. Pre-change the branch returned sweepBlocked, which diffSuburb short-circuits, so dropping listings was harmless.\n\nSecondary claim also holds: *blockCounter++ is skipped, so blockedSweeps stays 0 (crawl_listings.go:398) and agentJobTerminal (crawl_agent.go:870-883) banks the job as \"succeeded\".\n\nRefutation attempts failed: no empty-set guard anywhere (grep of len(sweep.listings) finds only stats/log/telemetry); finish/finishSweep don't backfill from matched; the branch predicate (onTargetResults > 0 && onTargetResults <= len(matched)) guarantees >=1 real listing is discarded whenever it fires; only MIN_PER_PAGE=1 makes it unreachable, and at the default 5 it fires in exactly the thin-suburb case it was written for. New TestSweep_PageClassificationMatrix (crawl_listings_test.go:548+) asserts status + block counter only, never len(sw.listings), unlike TestSweep_CompleteOnEmptyPage (line 492).\n\nSeverity blocker retained: unconditional data loss every firing (listings never persisted, absence counters never reset, job reported succeeded), plus wrongful-delist corruption once prior active rows exist for that (source, region) and two consecutive thin runs occur \u2014 a direct violation of the delist-safety invariant documented at crawl_listings_diff.go:14-19. Proposed fix (populate collected from matched before the page-1 thin branch; add len(sw.listings) assertions) is correct."
   }
  },
  {
   "target": "crawl",
   "finding": {
    "severity": "major",
    "file": "services/house-price-collector/crawl_brandbrain.go",
    "line": "128, 194-201",
    "title": "F35 projection allowlists field NAMES but passes VALUES through verbatim \u2014 an arbitrary string (address/agent/listing id/raw HTML) still crosses to brandbrain",
    "detail": "`walkBrandbrainAggregates` copies `child` unchanged into the outbound contract (line 128) whenever `brandbrainAggregateName(key) != \"\"` and `isBrandbrainAggregateScalar(child)` is true \u2014 and `isBrandbrainAggregateScalar` (line 194) accepts `string` and `bool`, unbounded in length and content. Name matching is fuzzy `strings.Contains` (lines 171-191): any key containing `median`+`price` (and not `rent`), or `daysonmarket`, `rentalyield`, `clearance`+`rate`, `annualgrowth`, `pricegrowth`, matches \u2014 so `medianPriceDisclaimer`, `medianSoldPriceCommentary`, `daysOnMarketBlurb`, `medianPriceWidgetHtml` all qualify, anywhere in any script blob on the page, at any nesting depth (including inside per-listing objects and inside JSON-in-string values).\n\nSo the boundary is a key allowlist with a value passthrough, not a value allowlist. Verified against the built code (`go test -overlay`, nothing written to the repo):\n\n  input:  {\"suburbProfile\":{\"medianPriceNote\":\"Sold by Jane Agent of 12 Smith St for $2,100,000\",\"daysOnMarketLabel\":\"listing-id-99887 at 4 Ocean Rd\"}}\n  output: {\"aggregate_fields\":[{\"name\":\"days_on_market\",\"value\":\"listing-id-99887 at 4 Ocean Rd\"},{\"name\":\"median_sale_price\",\"value\":\"Sold by Jane Agent of 12 Smith St for $2,100,000\"}]}\n\nEvery one of the five categories the change claims cannot cross (address, price, listing id, agent, raw HTML) crossed in that one call.\n\nThe added test `TestBrandbrain_ExtractRealEstate_SendsAggregateOnlyPayload` (crawl_brandbrain_test.go:97+) only checks a fixed list of forbidden literals against one synthetic fixture in which the private data sits under NON-matching keys (`listing.address`, `listing.agent.name`, `vendorSecret`). It therefore cannot catch this class at all: it never exercises a string value under an allowlisted name.\n\nNote the downstream consumer needs none of this: every field of `reaListing` (crawl_brandbrain.go:61-68) is `float64`, and the count fields are integers, so admitting `string`/`bool` buys nothing while widening the contract.\n\nThe CLAUDE.md (line ~493) and docs/housing-architecture.md (line 250) sentences added by this same commit \u2014 \"raw portal HTML, listing IDs, addresses, prices and agents stay on the rig\" \u2014 therefore assert a guarantee the code does not enforce.",
    "failure_scenario": "REA/Domain ships (or A/B-tests) a suburb-profile key whose name fuzzy-matches the allowlist but whose value is prose or markup \u2014 e.g. `medianPriceDisclaimer: \"Based on 3 sales incl. 12 Smith St, sold by Jane Agent of Ray White for $2.1m\"` or `medianPriceWidgetHtml: \"<div class=...>...</div>\"`. `brandbrainMediansPayload` emits it as `{\"name\":\"median_sale_price\",\"value\":\"<that whole string>\"}` and `extractRealEstate` POSTs it to api.brandbrain.dev. Address, agent name, an asking price and raw HTML leave the rig, in the exact tier the change was written to seal. No test fails.",
    "fix": "Restrict `isBrandbrainAggregateScalar` to `float64` (parse money strings locally with the existing `parseMoney`/`toMoney` before emitting), or clamp string values to a numeric/enum shape plus a hard length cap. Add a table-driven test whose cases put hostile strings UNDER allowlisted names, and prefer exact key matching over `strings.Contains`."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "major",
    "note": "CONFIRMED \u2014 mechanism reproduced against the built code; the reviewer did not misread it, and no guard exists elsewhere in the diff.\n\nReproduction (go test -overlay, nothing written to the repo; `git status --porcelain` clean before and after) in /Users/benebsworth/projects/.worktrees/shorted-hw-crawl-correctness/services/house-price-collector:\n\n  in : <script type=\"application/json\">{\"suburbProfile\":{\"medianPriceNote\":\"Sold by Jane Agent of 12 Smith St for $2,100,000\",\"daysOnMarketLabel\":\"listing-id-99887 at 4 Ocean Rd\",\"medianPriceWidgetHtml\":\"<div class=\\\"x\\\">raw markup</div>\",\"clearanceRateFlag\":true}}</script>\n  out: {\"aggregate_fields\":[{\"name\":\"clearance_rate\",\"value\":true},{\"name\":\"days_on_market\",\"value\":\"listing-id-99887 at 4 Ocean Rd\"},{\"name\":\"median_sale_price\",\"value\":\"Sold by Jane Agent of 12 Smith St for $2,100,000\"},{\"name\":\"median_sale_price\",\"value\":\"<div class=\\\"x\\\">raw markup</div>\"}]}\n\nAlso reproduced with the hostile string nested INSIDE a per-listing object (`results.exact.items[0].medianSoldPriceCommentary`) \u2014 it crossed, while the sibling `address`/`agent.name` keys on the same object did not. Address, agent, an asking price and raw markup all crossed in one call, exactly as claimed.\n\nCode facts that hold up: crawl_brandbrain.go:123-129 appends `child` verbatim; `isBrandbrainAggregateScalar` (194-201) accepts `string`/`bool` with no shape or length constraint; `brandbrainAggregateName` (171-191) is fuzzy `strings.Contains` over a key normalized to [a-z0-9], applied at every depth including inside JSON-in-string values (137-145). So the boundary is a key allowlist with value passthrough.\n\nReachability is stronger than the \"dormant tier\" comment at crawl_brandbrain.go:89 implies: crawl.go:349 (`crawlSource` \u2192 `extractRealEstate`) is invoked from `crawlSuburb` \u2190 `runCrawl` \u2190 `-mode crawl` (main.go:61), and `services/house-price-collector/deploy/run-housing-crawl.sh:173` runs `\"$BIN\" -mode crawl` unconditionally after every warm check, alongside `-mode listings`. `Dockerfile.crawl` ENTRYPOINT is `-mode crawl`. Dry-run does NOT gate it: `runCrawl` (crawl.go:277-289) only skips the DB upsert; the brandbrain POST happens first, regardless.\n\nTest-coverage claim confirmed: in `TestBrandbrain_ExtractRealEstate_SendsAggregateOnlyPayload` (crawl_brandbrain_test.go:100+) every forbidden literal sits under a NON-matching key (`listing.id`, `listing.address`, `listing.price`, `listing.agent.name`, `vendorSecret`, and `address` inside the `encoded` JSON-string). No case puts a string under an allowlisted name, so the test passes by construction and cannot catch this class.\n\nDocs claim confirmed as an overclaim added by this same commit: CLAUDE.md line ~493 and docs/housing-architecture.md:250 (\"raw portal HTML, listing IDs, addresses, prices and agents stay on the rig\") state an unconditional guarantee the code does not enforce. Proposed-fix feasibility also confirmed: `reaListing` (crawl_brandbrain.go:61-68) is all `float64`, and `parseMoney`/`toMoney` already exist locally (crawl_extract.go:64-98) to normalize \"$1.25m\" strings on the rig before emitting \u2014 so restricting to float64 costs nothing.\n\nTwo honest caveats that bound (but do not refute) it:\n1. Neither committed real-page fixture leaks today \u2014 `testdata/rea-pagemeta.html` projects to `{page_size:25, listings_total:63, total_pages:39, total_results:969}` and `domain-pagemeta.html` to `{page_size:20, total_results:608, total_pages:30}`, all numeric. The concrete leak therefore depends on REA/Domain shipping a fuzzy-matching key with a prose/markup value \u2014 plausible (portals routinely ship display strings beside numbers, and the author cites schema drift as the very reason for no fallback) but not demonstrated on captured pages. That is why the value-shape gate matters: the code must be safe under drift it cannot see.\n2. The recipient is first-party (api.brandbrain.dev on our own droplet), and the parallel consolidated copy at services/jobs/internal/jobs/houseprices/crawl_brandbrain.go:83-92 (registered via services/jobs/cmd/shorted/main.go:47, untouched by this diff) still POSTs the FULL raw HTML \u2014 so the repo-wide guarantee the new docs assert is broken in a second, larger place too. Worth a separate follow-up; it does not excuse the reviewed file, which is the one the docs point at and the one the launcher runs.\n\nSeverity: keeping major. The change exists solely to seal this boundary, the path runs live on every launcher invocation against adversarial pages whose payloads change without notice, the passthrough is unbounded in length and content, no test can catch the class, and the fix is a one-line type restriction plus a table-driven test with hostile strings under allowlisted names."
   }
  },
  {
   "target": "crawl",
   "finding": {
    "severity": "minor",
    "file": "docs/housing-architecture.md",
    "line": "250, 280",
    "title": "Doc edits land in a file that a parallel branch has already reduced to a redirect stub \u2014 the edits will conflict or be silently lost",
    "detail": "This commit edits two paragraphs inside `docs/housing-architecture.md`. On the in-flight docs branch `docs/housing-feature-docs` (currently checked out in the primary worktree) that same file is already a 567-byte redirect stub whose body reads \"This file is a redirect stub kept only so older links resolve \u2014 do not add content here\", and the monolith has moved to `docs/feature/housing/architecture.md` (77KB). Both edited paragraphs exist there verbatim and UNEDITED, at `docs/feature/housing/architecture.md:262` (\"fail-safe by design \u2026\") and `:292` (\"crawlJobSummary: suburbs, listings, events, blocked_sweeps, needs_rewarm\").\n\nSo whichever branch merges second either conflicts on those hunks or, if the stub-ifying delete wins, drops these two content edits entirely \u2014 and the new canonical doc keeps describing the pre-change brandbrain boundary.",
    "failure_scenario": "docs/housing-feature-docs merges first (it deletes ~77KB from docs/housing-architecture.md). This branch then merges: git reports a modify/delete-style conflict on both hunks, or a resolver takes the stub and the two paragraphs are lost. docs/feature/housing/architecture.md:262 and :292 continue to state that the medians tier hands brandbrain the rendered HTML, contradicting the shipped code.",
    "fix": "Re-apply the two paragraph edits to docs/feature/housing/architecture.md:262 and :292 (and drop the docs/housing-architecture.md hunks), or rebase onto docs/housing-feature-docs before merging."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "CONFIRMED \u2014 every factual claim checks out, and the conflict is empirically proven rather than predicted.\n\nEVIDENCE\n1. Branch is real and in-flight: `docs/housing-feature-docs` (commit 259c2ba22, 2026-08-09) is checked out in the primary worktree /Users/benebsworth/projects/shorted, exactly 1 commit ahead of origin/main. It is UNPUSHED (`git ls-remote --heads origin | grep housing-feature-docs` \u2192 no match) with no open PR (`gh pr list` shows only #408, #292, #104, #103). That is the strongest refutation angle and it FAILS: the same commit adds `.claude/housing-program/crawl-correctness.md` \u2014 the dispatch spec that produced the very branch under review \u2014 so both branches are products of one active program.\n2. Stub confirmed: `git show docs/housing-feature-docs:docs/housing-architecture.md | wc -c` = 567 bytes, body ends \"This file is a redirect stub kept only so older links resolve \u2014 do not add content here.\" Monolith moved to docs/feature/housing/architecture.md (78,858 bytes). origin/main still carries the 77,529-byte monolith and has no docs/feature/housing/ tree.\n3. Line numbers are exact: sed -n '258,266p' puts the \"fail-safe by design\u2026\" paragraph at line 262; sed -n '288,296p' puts the crawlJobSummary paragraph at line 292. Both verbatim and UNEDITED.\n4. Conflict PROVEN, not predicted: `git merge-tree --write-tree --name-only docs/housing-feature-docs feat/housing-crawl-correctness` exits 1 \u2192 \"CONFLICT (content): Merge conflict in docs/housing-architecture.md\". Three-way merge off the shared base 8c120a352, so whichever lands second conflicts regardless of order.\n5. The doc edit is load-bearing: crawl_brandbrain.go really does replace the raw-HTML hand-off with `brandbrainMediansPayload` (goquery walk over script JSON blobs \u2192 allowlisted aggregate/count projection, no raw-page fallback by design).\n6. The doc gap is real: grepping ALL of docs/feature/housing/ for `ExtractRealEstate`, `rendered.*HTML`, `allowlisted` returns ZERO hits. The split docs document the listings-tier counts-only queue boundary (README.md:52, data-sources.md:113, pipeline.md:54/87/92) but carry no record of the medians-tier boundary whatsoever.\n\nTWO CORRECTIONS \u2014 both make the finding UNDERSTATED, not wrong:\n(a) Scope is broader than reported. merge-tree also reports \"CONFLICT (content): Merge conflict in CLAUDE.md\" \u2014 the docs branch rewrites the entire Housing section (-65/+34) including the exact \"Tier-3 residential listing crawl\" paragraph this commit edits. The reviewer flagged only docs/housing-architecture.md; the fix must cover the CLAUDE.md hunks too.\n(b) It is a modify/modify content conflict, not modify/delete \u2014 the stub file still exists. The reviewer hedged (\"modify/delete-style\"), which is loose but immaterial.\n\nONE OVERSTATEMENT (degree, not substance): the failure scenario says docs/feature/housing/architecture.md:262 and :292 \"continue to state that the medians tier hands brandbrain the rendered HTML, contradicting the shipped code.\" They do not STATE that \u2014 they OMIT the new guarantee entirely (zero ExtractRealEstate hits anywhere in the split docs, and none on main's monolith either). So it is a documentation gap, not an active contradiction. The actionable consequence is unchanged: the new canonical doc would carry no record of the aggregate-only medians boundary this commit was written to document.\n\nSEVERITY: minor is correct, unchanged. Documentation-only, zero runtime/production impact, both branches pre-merge and local. Worst case is a merge conflict a human resolves loudly, or two paragraphs silently dropped if a resolver takes the stub side. Fix is cheap and should happen pre-merge: re-apply both paragraph edits to docs/feature/housing/architecture.md:262 and :292 plus the rewritten CLAUDE.md Housing section, or rebase this branch onto docs/housing-feature-docs.\n\nRead-only: nothing modified; the trial merge used merge-tree, which writes no working-tree or ref state."
   }
  },
  {
   "target": "crawl",
   "finding": {
    "severity": "minor",
    "file": "services/house-price-collector/crawl_playwright_test.go",
    "line": "97",
    "title": "The 'nothing forwarded to brandbrain' assertion is now unreachable, and an empty projection still burns the full 3-attempt RPC with no distinguishing log",
    "detail": "`brandbrainMediansPayload` (crawl_brandbrain.go:92) always returns at least `{\"aggregate_fields\":[]}` \u2014 confirmed by dumping the payload \u2014 so `req.HTML` can never be the empty string. The orchestration test's guard `if req.HTML == \"\" { t.Errorf(\"brandbrain: empty aggregate projection forwarded\") }` therefore can no longer fire, and the coverage it used to provide (the fetcher forwarded nothing) is gone.\n\nThe production side has the same blind spot: `extractRealEstate` marshals and POSTs unconditionally, so total key-schema drift on REA/Domain yields a zero-field contract sent per source per suburb through the 3-attempt / 90s-timeout client, and the only downstream signal is `cr.stats.rejected++` in crawlSource \u2014 indistinguishable from \"the LLM found nothing on a real page\".",
    "failure_scenario": "REA renames its ArgonautExchange keys so no key matches the allowlist. Every `-mode crawl` suburb POSTs `{\"aggregate_fields\":[]}` to api.brandbrain.dev (2 requests/suburb, retried on 5xx), the extractor returns nothing, `cr.stats.rejected` climbs, and no log line says the local projection was empty rather than the extraction. The unit suite stays green because the only assertion that could have caught an empty forward is now dead code.",
    "fix": "Skip the RPC (and log once) when the projection yields zero aggregate fields, and change the test guard to assert the decoded contract has at least one aggregate_fields entry rather than comparing req.HTML to \"\"."
   },
   "verdict": {
    "verdict": "CONFIRMED",
    "severity": "minor",
    "note": "Verified in /Users/benebsworth/projects/.worktrees/shorted-hw-crawl-correctness.\n\n1) Dead assertion \u2014 CONFIRMED. services/house-price-collector/crawl_brandbrain.go:93 initialises `contract := brandbrainMediansContract{AggregateFields: []brandbrainAggregateField{}}` (non-nil empty slice) and line 106 marshals unconditionally; a standalone marshal of the identical struct yields `{\"aggregate_fields\":[]}` (23 bytes, err=nil). Marshal cannot fail \u2014 values come from json.Unmarshal into `any` and are gated to float64/string/bool by isBrandbrainAggregateScalar, so no NaN/Inf. goquery parse failure also falls through to the same marshal. Therefore extractRealEstateReq.HTML is never \"\", and the guard at crawl_playwright_test.go:96-98 is unreachable in all five brandbrainServer call sites.\n\n2) Lost coverage \u2014 CONFIRMED empirically. `go test -run TestCrawlSuburb_RoutesBothSourcesToBrandbrain` PASSES with the script-free fixtures at crawl_playwright_test.go:110-111, i.e. an empty projection is forwarded twice and nothing complains. All non-skipped orchestration fixtures (110-111, 197-199, 219-220, 315-316) are script-free; the only realistic-HTML orchestration test (TestCrawlSource_WithRealFixtureHTML:438) t.Skip()s without an uncommittable ~/projects/housing-fixtures file. Pre-diff the guard had a real target (raw string(html)); post-diff it has none.\n\n3) Production blind spot \u2014 CONFIRMED by reading crawl.go:349-366: extractRealEstate is called unconditionally after outcomeOK, with no emptiness check and no log of the projection (grep finds no aggregate-related log line). On zero observations the sole trace is cr.stats.rejected++; crossCheck (crawl.go:390-400) early-returns when bbHouse==0 so it adds nothing in this exact case. `-mode crawl` is live on the rigs (deploy/run-housing-crawl.sh:173).\n\nNuances that trim but do not refute:\n- \"full 3-attempt RPC\" only applies on 5xx/transport failure (crawl_brandbrain.go:229-268); a 200 is a single request. The failure scenario's own wording (\"2 requests/suburb, retried on 5xx\") is accurate.\n- The zero-field precondition is stronger than described: testdata/rea-pagemeta.html and domain-pagemeta.html both carry allowlisted count keys (totalResultsCount, listings_total, maxPageNumberAvailable, totalPages, totalListings, actualTotalResults, pageSize), so the count keys must drift alongside the median keys before the projection is truly empty.\n- The proposed test fix needs fixture changes too: asserting >=1 aggregate_fields entry inside brandbrainServer would fail 4 of the 5 current call sites, whose canned HTML has no <script>.\n\nSeverity stays minor: this is a test-coverage/observability gap plus a bounded wasted-RPC path in a dormant tier. No data-correctness impact \u2014 the capital-band gate, sanity bounds and source_licence tagging are untouched, and an empty projection can only cost an observation, never store a bad one."
   }
  }
 ],
 "refuted": [
  {
   "target": "mv",
   "finding": {
    "severity": "major",
    "file": "services/migrations/000109_fix_listing_rollup_correctness.up.sql",
    "line": "217-235",
    "title": "State rollup minus published suburb rollups isolates exactly the addresses the suburb-level k=3 floor suppressed",
    "detail": "mv_state_price_drops and mv_suburb_price_drops aggregate the identical underlying set (same 30-day window, same drop_pct<=0.40 cap, same active+addressable filter, same MAX-per-address) but suppress at different granularities: the state row publishes dropped_count and dropped_value for the whole state, while the suburb rows publish them only for suburbs with >=3 dropped addresses. Both are served by ungated public RPCs. Subtracting the sum of published suburb rows from the state row therefore yields the total (and the exact count) of everything below the floor. Verified on postgres:16: state NSW dropped_count=10 / dropped_value=1,035,000 minus the published suburbs (6 \u2192 550,000 and 3 \u2192 335,000) leaves count=1 and value=150,000 \u2014 the exact dollar cut on the single suppressed address in a 1-listing suburb. The same construction applies to the 12-month sold aggregates added by this migration (mv_suburb_listing_stats.sold_count/avg_sold at lines 59-68 vs mv_state_price_drops sold_count/avg_sold at lines 229-231), and there it is strictly worse: individual sale prices are not published on any other surface, not even the flag-gated address board (which is drops-only), so this is the only path by which a single dwelling's crawled sale price becomes derivable.",
    "failure_scenario": "Call GetPriceDropsOverview and ListSuburbPriceDrops (both public, both unaffected by HOUSING_DROP_LISTINGS_ENABLED). For a state where exactly one crawled suburb falls under the floor, count_state \u2212 \u03a3count_suburbs = 1 and value_state \u2212 \u03a3value_suburbs = that one address's exact price cut; with sold_count/avg_sold the same subtraction yields one dwelling's exact sale price.",
    "fix": "Make the levels consistent: either compute the state row only from suburbs that clear the floor (so the residual is structurally zero), or suppress the state row's value/mean whenever the residual behind it is below k."
   },
   "verdict": {
    "verdict": "REFUTED",
    "severity": "minor",
    "note": "Refuted as a defect of this diff; the load-bearing claims are wrong or already covered.\n\n(1) MISATTRIBUTED \u2014 the sold aggregates are NOT \"added by this migration\". `mv_suburb_listing_stats.sold_count/avg_sold/median_sold` exist in services/migrations/000077_add_suburb_listing_stats.up.sql:16-30 and the state-level ones in services/migrations/000086_price_drops_rollups.up.sql:135-140, both with NO k floor and no window (000109's own .down.sql restores exactly that shape). Pre-change, a suburb with one sold row publishes `avg_sold` = that dwelling's exact sale price DIRECTLY via the public ungated ListSuburbPriceDrops. Line 65-66 of 000109 (`CASE WHEN sold.sold_count >= 3 THEN sold.avg_sold`) is the first suppression this subsystem has had, so the claim \"strictly worse \u2026 this is the only path by which a single dwelling's crawled sale price becomes derivable\" is inverted: the diff closes the direct path and leaves only a harder derived residual.\n\n(2) PRE-EXISTING for drops \u2014 000086:151 published state `dropped_value` unconditionally while the suburb MV already carried `WHERE a.dropped_listing_count >= 3` (000086:80). The exact state\u2212suburb residual described is live on prod today; 000109:222 ADDS the `>= 3` gate on the state row. Nothing new is introduced at lines 217-235.\n\n(3) ALREADY PUBLISHED for drops \u2014 ListAddressPriceDrops is VISIBILITY_PUBLIC (proto/shortedapi/shorts/v1alpha1/housing.proto:52-54) and gated only by `dropListingsEnabled()`, which returns true unless the env is explicitly falsey (services/shorts/internal/services/shorts/house_prices.go:402-414 \u2014 kill switch, ON by default). It returns display_address + first_price + current_price + drop_abs + drop_pct per address (limit 200, sortable, state-filtered, 365-day window). An unattributed residual of \"one address in NSW cut $150,000\" is strictly less than the board that publishes the address next to the dollar figure.\n\n(4) NO SUBURB ATTRIBUTION for drops \u2014 below-floor suburbs come back as COALESCE(d.dropped_listing_count, 0) in ListSuburbPriceDrops (postgres_house_prices.go:698-712), indistinguishable from genuinely zero-drop suburbs, so the residual cannot be pinned to a suburb.\n\n(5) RESIDUAL IS NOT EXACT on real data \u2014 the state MV filters property_listings.state_code, the suburb board filters house_price_regions.state_code through an INNER JOIN (postgres_house_prices.go:708-712), and the suburb MV has no state filter at all, so the residual also absorbs unmapped-region and empty-state rows. The reviewer's postgres:16 reproduction used a hand-built partition where those channels are empty.\n\nWhat genuinely survives (severity minor, hardening not defect): the classic k-anon differencing residual on the SOLD measure \u2014 suburb `sold_count` is still published unsuppressed, so a lone sold_count=1 suburb is visible and, when it is the only sub-floor suburb in a state, state avg_sold\u00d7sold_count minus the published suburb products recovers that one sale price. Worth noting as a follow-up (e.g. suppress the coarser mean when the residual behind it is below k), but it is pre-existing in kind, data-configuration dependent, and materially better after this diff than before it.\n\nFiles read: services/migrations/000109_fix_listing_rollup_correctness.{up,down}.sql, 000086_price_drops_rollups.up.sql, 000077_add_suburb_listing_stats.up.sql, services/shorts/internal/store/shorts/postgres_house_prices.go, services/shorts/internal/services/shorts/house_prices.go, proto/shortedapi/shorts/v1alpha1/housing.proto."
   }
  }
 ]
}