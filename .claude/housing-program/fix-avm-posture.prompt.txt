Revise the AVM (property.com.au valuation) posture on this branch (feat/housing-api-hardening). Your previous commit made HOUSING_VALUATIONS_ENABLED an explicit truthy-only OPT-IN, defaulting OFF. That is the wrong mechanism, though the instinct was right. Change it as follows.

WHY THE CURRENT APPROACH IS WRONG: this repo has a standing rule against opt-in "dark" flags — features ship ON by default with an explicit KILL SWITCH only. A flag that defaults off leaves built code dark, and worse, it leaves a live path someone can re-enable with one env var, silently re-creating the exact licence exposure migration 000088 was written to prevent.

WHAT 000088 ACTUALLY REQUIRES (read services/migrations/000088_*.up.sql, lines 9-14): "the harvested profile is proprietary/ToS-restricted (property.com.au's robots.txt explicitly bans aggregators) and MUST NEVER be republished raw. Only DERIVED aggregates are a publishable surface; the raw profile is stored for internal enrichment only." The AVM estimate_low/mid/high ARE the raw profile values, so rendering them on a public page is republishing raw — no amount of attribution, deep-linking or "model estimate" disclaimer changes that. This is the same rule the listings tier already follows: /price-drops publishes derived aggregates, never raw listing rows.

WHAT TO IMPLEMENT (the "internal enrichment only" posture):
1. Remove the valuation from the PUBLIC read path entirely. GetPropertyHistory must not populate the `valuation` field. Do NOT gate it behind a flag — remove the public path so it cannot be re-enabled by configuration. Leave a short comment at the removal site citing 000088's "internal enrichment only" line so the next reader understands it is a licence constraint, not an oversight.
2. Delete the now-dead valuationsEnabled() helper and its env var references, including in tests and docs. Do not leave a dark flag behind. (Leave dropListingsEnabled() exactly as it is — ON by default with a kill switch. Our own observations of publicly-posted asking prices are ours; that distinction is the whole point.)
3. KEEP the harvest and the internal store method (GetPropertyValuation, the collector's property mode, the tables). This data remains valid for internal enrichment and future DERIVED aggregates — nothing about the pipeline changes.
4. Clean up the now-unreachable UI: PropertyValuationCard will never receive data. Remove its render site in property-history-view.tsx and delete the component and its test if nothing else uses them. The rest of the /housing/property/[addressKey] page (our own observed listing price-event history) STAYS — it is not affected.
5. Keep the proto field itself (do not edit .proto files); it simply goes unpopulated on the public path.
6. Update any doc you previously edited that describes the opt-in flag (docs/property-avm-read-path-plan.md, deploy READMEs) to describe the internal-enrichment-only posture instead.
7. Add/adjust a test asserting GetPropertyHistory does NOT return a valuation even when a property_valuations row exists — that is the regression guard.

Ground rules: add commits on top of your existing work; do not rewrite history, push, merge or switch branches. Re-run the scoped Go tests and the touched web tests and report ACTUAL output; if the sandbox blocks something, say so plainly. Finish with a summary of what you removed, what you deliberately kept, and test results.
