Continue and COMPLETE the wave-2 housing enhancement package "affordability-panel" on this branch (feat/housing-affordability-panel). A previous Codex run on this exact worktree was interrupted mid-flight; its partial work is ALREADY COMMITTED as a wip() commit on this branch.

FIRST: run `git log --oneline 8c120a352..HEAD` and `git diff 8c120a352..HEAD --stat`, and read the changed files, to establish exactly what your predecessor already did. Then read the spec at /Users/benebsworth/projects/shorted/.claude/housing-program/w2-affordability-panel.md and finish everything it requires that is not yet done.

Do not ask clarifying questions - you have full authority to make the design calls using the approved spec and the existing /housing design patterns (BigStat tiles, HousingSeriesChart, the amber series palette, existing source-attribution conventions). Text-only, no browser companion.

Do NOT redo completed work and do NOT revert it unless it is wrong. Add commits on top; do not rewrite history, push, merge or switch branches.

QA before finishing: `cd web && npx tsc --noEmit`, the touched jest suites, and `npm run bundle:budget` if the /housing route's JS grew. Report ACTUAL results; if the sandbox blocks something (font fetches, port binds), say so plainly rather than claiming it passed. Finish with a summary: what your predecessor had done, what you added, deliberate omissions, test results, and what needs hand-verification.
